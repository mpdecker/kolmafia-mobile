## Learned User Preferences

- Do not edit attached plan files during implementation; treat them as read-only reference.
- Do not recreate plan todos; mark existing ones in_progress and complete all before stopping.
- Do not git commit unless explicitly requested.
- Use the `gh` CLI for GitHub pull requests when asked.
- After phase implementation, update `docs/parity-audit.md` with metrics and a history entry.
- Phase planning and gap-closing work should follow `docs/parity-audit.md` Top Priorities.
- Short "pr" means commit current work and open or update a GitHub PR via `gh`.
- Short "push" means git push the current branch to remote.
- Diff-tab "commit-and-push" means commit current work and push to remote.
- "next pass", "next", or "next phase" means continue the next incremental parity phase from `docs/parity-audit.md` Top Priorities.
- Short "continue" means resume in-progress parity work or proceed with Top Priorities when between tasks.
- When committing, stage only files related to the current work; exclude unrelated or line-ending-only changes.

## Learned Workspace Facts

- Kotlin Multiplatform KoLmafia mobile port; core logic lives in `shared/src/commonMain`.
- Modules include `shared`, `androidApp`, and platform targets.
- Desktop KoLmafia Java reference source is at `C:\Development\kolmafia\kolmafia\src\`; ported data files live in `shared/src/commonMain/composeResources/files/data/`.
- `docs/parity-audit.md` is the authoritative desktop-vs-mobile parity document.
- Phase plans live under `docs/superpowers/plans/`; ASH batches in `GameRuntimeLibrary.AshP*Batch.kt` — AshP8–P18 registration floor, AshP19–AshP118 live behavioral batches (entity `to_*` resolvers, `$entity[field]` bracket reads via `*EntityFields.kt` including MONSTER/LOCATION/PATH fields + `$monster[base_mainstat_exp]` + `$monster[*_resistance]` + `$monster[raw_*]` + `$monster[base_*]` effective + `$monster[poison|group|manuel_name|wiki_name]` + `$monster[attack_elements]` + `$monster[sub_types|images]` + `$monster[random_modifiers|attributes]` + `$monster[fact|fact_type]` + `$monster[min_sprinkles|max_sprinkles]`, AshP39 `CombatAdjustment`, AshP40 drop/XP/initiative modifiers, AshP41–43 monster combat stats/element/`jump_chance`, AshP44 location `jump_chance`, AshP45 `meat_drop`/`item_drops`, AshP46 hit/miss combat APIs, AshP47 `all_monsters_with_id`, AshP48 jump Init polish, AshP49–52 expression Init/Atk/Def/HP eval, AshP53–54 numeric/expression Scale/Cap/Floor + `equipped()`, AshP55 Exp / `base_mainstat_exp`, AshP56 `REDUCE_ENEMY_DEFENSE`, AshP57 Phys/Elem resistance brackets, AshP58 `MLMult:` effective ML, AshP59 ML phys-res boost, AshP60 raw_* brackets, AshP61 base_* effective brackets, AshP62 `monster_initiative` initPenalty, AshP63 poison/group/manuel/wiki metadata, AshP64 multi-EA parse + attack_elements bracket, AshP65 sub_types/images brackets, AshP66 random_modifiers/attributes brackets, AshP67 fact/fact_type + FactDatabase, AshP68 min/max sprinkles + SprinkleMin/Max parse, AshP69 runtime random_modifiers + MonsterStatusTracker/OCRS parser, AshP70 RandomModifierStats lazy apply on last_monster, AshP71 EncounterModifierPipeline + leet name resolution, AshP72 monster beeosity (Beecore) on effective stats, AshP73 IntergnatDemonNameSync + demonName12 pref, AshP74 equipment beeosity (Maximizer/BHY), AshP75 SummoningChamberManager + summon CLI, AshP76 QuestLogConsequenceSync + demonName prefs, AshP77 YegDemonNameSync + cultshorts scrap syllables + demonName13 pref, AshP78 DemonInCombatNameSync + DemonName14Manager + demons CLI, AshP79 AlliedRadioManager + alliedradio CLI, AshP80 ConsequenceActionResolver + QUEST_LOG `SetExpressionValue` (`royalty` pref), AshP81 CargoPocketSync + `cargo` CLI + `picked_pockets`/`picked_scraps` ASH, AshP82 PocketDatabase + cargo count/list/typed-pick CLI + `pocket_monster`/`available_pocket`/`pick_pocket` ASH, AshP83 cargo pocket introspection (`potential_pockets`, `pocket_*`, list helpers) + `registerPocketFight`, AshP84 DESC_ITEM consequence expressions + `ItemDescriptionConsequenceSync` + `roman()`/`monstername`, AshP85 DESC_ITEM `=mods` + `ModifierEnchantmentParser`/`ItemEnchantmentParser` + `ModifierDatabase.overrideModifier` + `DynamicItemModifierSync`, AshP86 DESC_EFFECT consequence expressions + `=mods` + `EffectEnchantmentParser`/`EffectDescriptionConsequenceSync` + effect cached pref restore, AshP87 DESC_SKILL consequence expressions + `SkillDescriptionConsequenceSync` + desc_skill self=true hook, AshP88 COMBAT_SKILL consequence expressions + `CombatSkillConsequenceSync` + fight dropdown hook + `ExpressionContext` in `ConsequenceActionResolver`, AshP89 MONSTER disambiguation consequences + `MonsterConsequenceSync` + `ReturnReplacement` quoted actions, AshP90 dynamic modifier checkMods v1 + `checkDynamicModifiers` on login/refresh + KGB/Baseball owned desc visits, AshP91 consequence `updateOneDesc` rotation + `DescriptionConsequenceRegistry` + `DescriptionConsequenceSync`, AshP92 extended checkMods v2 (closet accessibility + saber/umbrella/wine/crimbo/ring/G9/zoot/heartstone + dart holster/mimic egg), AshP93 login desc checks v3 (crown/bjorn/bird-of-day/entauntauned/savage beast + closet KGB/Baseball), AshP94 skill-granting equipment conditional skills + coat-of-paint class-change refresh, AshP95 Eternity codpiece gem slots + EternityCodpiece modifiers + `have_skill` equipment merge, AshP96 crown/bjorn familiar desc sync (`Current Occupant` race parse), AshP97 Throne/Bjorn modifier accumulation in `CurrentModifiers` (worn crown/bjorn + enthroned/bjorned race), AshP98 `ItemAvailability`/`LimitModeGates` + `checkRing` itemAvailable wiring, AshP99 post-bird `SkillLearner.learnSkill` + `fetchSkills()` for Seek-a-Bird, AshP100 Eternity codpiece gem equip HTTP (`docodpiece` + choice 1588), AshP101 item access context (`buildCheckContext` storage/stash + Hermit clover gate + `AccessibleItemCount`), AshP102 battle `learnSkillFromResponse` (NEWSKILL parse + item consumption + leveled pref increment), AshP103 `AccessibleItemCount` depth (`PullableItems` LoL/Sea storage gates + worthless id 13 + Hat Trick hats + inactive familiar equipment), AshP104 `AccessibleItemCount` storage gates (`ThriftyRequest`/`StandardRequest` + `ItemRestriction.isAllowed` + ronin freepull split), AshP105 `StandardRequest.isAllowed` full parity (`TrendyRequest` + Thrifty evergreen + Quantum familiar + `ItemRestriction` wiring), AshP106 `FamiliarUsability`/`have_familiar` path gates + `RetrieveItemService` restricted-item early exit, AshP107 `in_terrarium` + Maximizer familiar switch usability + `RestrictionListRefresh` on retrieve, AshP108 `canUseFamiliars`/Quantum familiar gates + `use_familiar`/CLI/enthrone/bjornify usability + Maximizer enthrone/bjorn filtering, AshP109 CharacterState resource getters (`my_fury`/`my_pp`/`my_robot_*`/plumber stats) + `my_path_id` pathId fix + `EntityIntrospection` `name(entity)`/extended `to_int(entity)`, AshP110 charpane/combat class-resource sync (`ClassResourceCharpaneSync`/`ClassResourceCombatSync` + live `my_mask`/`my_paradoxicity`/`my_audience`/`my_discomomentum`/`telescope_upgrades`/`telescope_looked_high`), AshP111 campground garden sync (`GardenSync`/`CropType` + live `my_garden_type`), AshP112 closet+storage meat sync (`ClosetMeatSync`/`StorageMeatSync` + live `my_closet_meat`/`my_storage_meat`), AshP113 session meat tracking (`SessionMeatSync` + live `my_session_meat`), AshP114 desc(entity) description cache (`DescriptionCache` + cache-only item/effect/skill `desc()`), AshP115 storage pulls remaining sync (`pulls_remaining` + `ConcoctionDatabase` pulls state), AshP116 stills + mushroom plot ASH (`stills_available` + `StillsAvailability`/`StillSync`; `have_mushroom_plot` + `MushroomPlotSync`), AshP117 `craft_type(item)` concoction descriptions (`CraftTypeDescription` + `ConcoctionDatabase`), AshP118 `refresh_status`/`restore_hp`/`restore_mp` (`RecoveryManager.checkpointedRecoverHp/Mp` + api.php status refresh), modifier lookups via `modifiers/*`; roadmap: `2026-06-11-ash-script-full-parity.md`).
- Run unit tests with `.\gradlew.bat :shared:jvmTest`; verify Android builds with `.\gradlew.bat :androidApp:assembleDebug`.
- On Windows PowerShell, chain shell commands with `;`, not `&&`.
- Incremental parity phases use Quest/CLI/Maximizer tracks; Phases 53–160 advanced Tier 1 (AshP19–AshP115 entity/modifier/monster/combat libraries including MONSTER/LOCATION/PATH brackets, combat adjustment, drop/XP/initiative/meat/item drops, monster combat stats/element/`jump_chance`, location `jump_chance`, hit/miss APIs, `all_monsters_with_id`, expression Init/Atk/Def/HP, numeric + expression Scale/Cap/Floor, Exp/`base_mainstat_exp`, `REDUCE_ENEMY_DEFENSE`, Phys/Elem resistance brackets, `MLMult:` effective ML, ML phys-res boost, `$monster[raw_*]` / `$monster[base_*]` effective, `monster_initiative` initPenalty, `$monster[poison|group|manuel_name|wiki_name]`, `$monster[attack_elements]` multi-EA, `$monster[sub_types|images]`, `$monster[random_modifiers|attributes]` (runtime via MonsterStatusTracker + EncounterModifierPipeline), RandomModifierStats lazy apply on last_monster, monster beeosity (Beecore) on effective stats, Intergnat demonName12 sync from fight HTML, equipment beeosity (Maximizer/BHY), Summoning Chamber `summon` CLI, quest-log demonName sync, Yeg demonName13 cargo scrap sync, demonName14 segment tracking + `demons solve14` CLI, Allied Radio HTTP + `alliedradio` CLI, QUEST_LOG modifier-expression consequences (`royalty` pref via `ConsequenceActionResolver`), cargo-cult picked-pocket sync + `cargo` CLI + `picked_pockets`/`picked_scraps` ASH, PocketDatabase + cargo count/list/typed-pick + `pocket_monster`/`available_pocket`/`pick_pocket` ASH, cargo pocket introspection + `registerPocketFight`, DESC_ITEM consequence expressions + `ItemDescriptionConsequenceSync` + `roman()`/`monstername`, DESC_ITEM `=mods` enchantment parse + `ModifierDatabase.overrideModifier` + `DynamicItemModifierSync`, DESC_EFFECT consequence expressions + `=mods` + `EffectDescriptionConsequenceSync` + effect cached pref restore, DESC_SKILL consequence expressions + `SkillDescriptionConsequenceSync` + desc_skill self=true hook, COMBAT_SKILL consequence expressions + `CombatSkillConsequenceSync` + fight dropdown hook, MONSTER disambiguation consequences + `MonsterConsequenceSync` + `disambiguateMonster`, dynamic modifier checkMods v1 + `checkDynamicModifiers` on login/refresh, consequence `updateOneDesc` rotation prefetch, extended checkMods v2, login desc checks v3 (crown/bjorn/bird-of-day/entauntauned/savage beast + closet KGB/Baseball), skill-granting equipment conditional skills + coat-of-paint class-change refresh, Eternity codpiece `eternitycod` API + CODPIECE1–5 slots + gem modifiers + codpiece conditional skills + `have_skill` equipment merge, crown/bjorn familiar desc sync (`Current Occupant` race parse), Throne/Bjorn modifier accumulation in `CurrentModifiers`, `ItemAvailability`/`LimitModeGates` itemAvailable gates, post-bird `SkillLearner.learnSkill` + `fetchSkills()`, Eternity codpiece gem equip HTTP (`EquipmentRequest` docodpiece + choice 1588), `buildCheckContext` storage/stash IDs + Hermit clover coinmaster gate + `AccessibleItemCount` for `available_amount`/retrieve check-only, battle `learnSkillFromResponse` (NEWSKILL parse + item consumption + leveled pref increment), `AccessibleItemCount` depth (LoL/Sea storage gates + worthless id 13 + Hat Trick + inactive familiar equipment), `AccessibleItemCount` storage gates (Thrifty/Standard restriction + ronin freepull split), `StandardRequest.isAllowed` full parity (Trendy/Thrifty evergreen/Quantum familiar), `FamiliarUsability`/`have_familiar` path gates + retrieve restricted-item early exit, `in_terrarium` ownership-only ASH + Maximizer familiar switch usability + `RestrictionListRefresh` on retrieve, `canUseFamiliars`/Quantum gates + `use_familiar`/CLI/enthrone/bjornify usability + Maximizer enthrone/bjorn filtering, AshP109 CharacterState resource getters + `my_path_id` + `EntityIntrospection` name/extended to_int, AshP110 charpane/combat class-resource sync, AshP111 campground garden sync (`GardenSync` + live `my_garden_type`), AshP112 closet+storage meat sync + AshP113 session meat tracking (`SessionMeatSync` + live `my_session_meat`), AshP114 desc(entity) cache-only description lookups (`DescriptionCache` + live `desc()` for item/effect/skill), AshP115 storage pulls remaining sync (`pulls_remaining` + `StorageMeatSync` pullsleft parse), AshP116 stills + mushroom plot ASH (`stills_available` gate logic + `StillSync`; `have_mushroom_plot` + `MushroomPlotSync`), AshP117 `craft_type(item)` concoction method descriptions (`CraftTypeDescription`), AshP118 `refresh_status`/`restore_hp`/`restore_mp` (`RecoveryManager.checkpointedRecoverHp/Mp`), `$monster[fact|fact_type]`, `$monster[min_sprinkles|max_sprinkles]`, CLI long-tail including `journey`/`witchess`/`volcano`, charpane sync for Ed/VYKEA/pasta thrall/class resources, tower/hedge maze/PirateRealm sync); `GameRuntimeLibrary.REVISION` uses `phaseNN` format (currently `phase160`); update `docs/parity-audit.md` after each phase.
- Quest state sync combines QuestAdvanceRules, QuestChoiceRules, QuestFightRules, QuestItemRules, QuestLogSync, QuestSpecialSync, and dedicated `quest/*Sync.kt` visit parsers (e.g. TowerSync, TelescopeSync, PirateRealmSync); Ed servants in `servant/EdServantManager.kt` with `EdServantChoiceSync`/`EdServantCharpaneSync`; VYKEA companion in `vykea/VykeaCompanionManager.kt` with `VykeaCharpaneSync`; pasta thrall in `thrall/PastaThrallManager.kt` with `PastaThrallCharpaneSync`; class/path resources in `character/ClassResourceCharpaneSync.kt`/`ClassResourceCombatSync.kt`; campground garden in `campground/GardenSync.kt`; still shop + mushroom plot in `concoction/StillSync.kt`/`campground/MushroomPlotSync.kt`; closet/storage/session meat in `inventory/ClosetMeatSync.kt`/`StorageMeatSync.kt`/`SessionMeatSync.kt`; entity description cache in `data/DescriptionCache.kt`; bundled data loaders: `journeyman.txt` via `data/JourneymanDatabase.kt`, `witchess_solutions.txt` via `data/WitchessSolutionDatabase.kt`/`session/WitchessManager.kt`, `volcanomaze.txt` via `data/VolcanoMazeDatabase.kt`/`session/VolcanoMazeManager.kt`, `cultshorts.txt` via `data/PocketDatabase.kt`/`data/CultShortsDatabase.kt`/`session/YegDemonNameSync.kt`; tower automation in `TowerDoorRunner.kt`/`TowerDoorConfig.kt`; hedge maze in `HedgeMazeRunner.kt`/`HedgeMazeConfig.kt`.
- ASH runtime functions register via `regFn()` in `GameRuntimeLibrary.*.kt` under `ash/`; AshScope uses first-match overload resolution — remove AshP11 stubs and wire live lookups in AshP12–P18 `resolveModifier*` helpers when adding AshP19+ behavioral batches.
- `AshCompatibilityCorpusTest` is the behavioral ASH regression corpus; JavaScript runtime and relay server are explicit parity non-goals.
- Git default branch is `master`; avoid stale `.worktrees/` for active development; exclude `.cursor/` hook state from commits unless requested.
