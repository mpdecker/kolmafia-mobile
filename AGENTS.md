## Learned User Preferences

- Do not edit attached plan files during implementation; treat them as read-only reference.
- Do not recreate plan todos; mark existing ones in_progress and complete all before stopping.
- Do not git commit unless explicitly requested.
- Use the `gh` CLI for GitHub pull requests when asked.
- After phase implementation, update `docs/parity-audit.md` with metrics and a history entry.
- Phase planning and gap-closing work should follow `docs/parity-audit.md` Top Priorities.
- Short "pr" means commit current work and open or update a GitHub PR via `gh`.
- Short "push" means git push the current branch to remote.
- Diff-tab "commit-and-push" means commit current work and push to remote.
- "next pass", "next", "next phase", or "next major phase" means continue the next incremental parity phase from `docs/parity-audit.md` Top Priorities.
- Short "continue" means resume in-progress parity work or proceed with Top Priorities when between tasks.
- When committing parity work, stage all relevant changed files across accumulated phases in the working tree (not just the latest phase); exclude unrelated, `.cursor/`, `.worktrees/`, `tools/`, and line-ending-only changes.

## Learned Workspace Facts

- Kotlin Multiplatform KoLmafia mobile port; core logic lives in `shared/src/commonMain`.
- Modules include `shared`, `androidApp`, and platform targets.
- Desktop KoLmafia Java reference source is at `C:\Development\kolmafia\kolmafia\src\`; ported data files live in `shared/src/commonMain/composeResources/files/data/`.
- `docs/parity-audit.md` is the authoritative desktop-vs-mobile parity document.
- Phase plans live under `docs/superpowers/plans/`; ASH batches in `GameRuntimeLibrary.AshP*Batch.kt` — AshP8–P18 registration floor, AshP19–AshP216 live behavioral batches (entity `to_*` resolvers, `$entity[field]` bracket reads via `*EntityFields.kt` including MONSTER/LOCATION/PATH fields + `$monster[base_mainstat_exp]` + `$monster[*_resistance]` + `$monster[raw_*]` + `$monster[base_*]` effective + `$monster[poison|group|manuel_name|wiki_name]` + `$monster[attack_elements]` + `$monster[sub_types|images]` + `$monster[random_modifiers|attributes]` + `$monster[fact|fact_type]` + `$monster[min_sprinkles|max_sprinkles]`, AshP39 `CombatAdjustment`, AshP40 drop/XP/initiative modifiers, AshP41–43 monster combat stats/element/`jump_chance`, AshP44 location `jump_chance`, AshP45 `meat_drop`/`item_drops`, AshP46 hit/miss combat APIs, AshP47 `all_monsters_with_id`, AshP48 jump Init polish, AshP49–52 expression Init/Atk/Def/HP eval, AshP53–54 numeric/expression Scale/Cap/Floor + `equipped()`, AshP55 Exp / `base_mainstat_exp`, AshP56 `REDUCE_ENEMY_DEFENSE`, AshP57 Phys/Elem resistance brackets, AshP58 `MLMult:` effective ML, AshP59 ML phys-res boost, AshP60 raw_* brackets, AshP61 base_* effective brackets, AshP62 `monster_initiative` initPenalty, AshP63 poison/group/manuel/wiki metadata, AshP64 multi-EA parse + attack_elements bracket, AshP65 sub_types/images brackets, AshP66 random_modifiers/attributes brackets, AshP67 fact/fact_type + FactDatabase, AshP68 min/max sprinkles + SprinkleMin/Max parse, AshP69 runtime random_modifiers + MonsterStatusTracker/OCRS parser, AshP70 RandomModifierStats lazy apply on last_monster, AshP71 EncounterModifierPipeline + leet name resolution, AshP72 monster beeosity (Beecore) on effective stats, AshP73 IntergnatDemonNameSync + demonName12 pref, AshP74 equipment beeosity (Maximizer/BHY), AshP75 SummoningChamberManager + summon CLI, AshP76 QuestLogConsequenceSync + demonName prefs, AshP77 YegDemonNameSync + cultshorts scrap syllables + demonName13 pref, AshP78 DemonInCombatNameSync + DemonName14Manager + demons CLI, AshP79 AlliedRadioManager + alliedradio CLI, AshP80 ConsequenceActionResolver + QUEST_LOG `SetExpressionValue` (`royalty` pref), AshP81 CargoPocketSync + `cargo` CLI + `picked_pockets`/`picked_scraps` ASH, AshP82 PocketDatabase + cargo count/list/typed-pick CLI + `pocket_monster`/`available_pocket`/`pick_pocket` ASH, AshP83 cargo pocket introspection (`potential_pockets`, `pocket_*`, list helpers) + `registerPocketFight`, AshP84 DESC_ITEM consequence expressions + `ItemDescriptionConsequenceSync` + `roman()`/`monstername`, AshP85 DESC_ITEM `=mods` + `ModifierEnchantmentParser`/`ItemEnchantmentParser` + `ModifierDatabase.overrideModifier` + `DynamicItemModifierSync`, AshP86 DESC_EFFECT consequence expressions + `=mods` + `EffectEnchantmentParser`/`EffectDescriptionConsequenceSync` + effect cached pref restore, AshP87 DESC_SKILL consequence expressions + `SkillDescriptionConsequenceSync` + desc_skill self=true hook, AshP88 COMBAT_SKILL consequence expressions + `CombatSkillConsequenceSync` + fight dropdown hook + `ExpressionContext` in `ConsequenceActionResolver`, AshP89 MONSTER disambiguation consequences + `MonsterConsequenceSync` + `ReturnReplacement` quoted actions, AshP90 dynamic modifier checkMods v1 + `checkDynamicModifiers` on login/refresh + KGB/Baseball owned desc visits, AshP91 consequence `updateOneDesc` rotation + `DescriptionConsequenceRegistry` + `DescriptionConsequenceSync`, AshP92 extended checkMods v2 (closet accessibility + saber/umbrella/wine/crimbo/ring/G9/zoot/heartstone + dart holster/mimic egg), AshP93 login desc checks v3 (crown/bjorn/bird-of-day/entauntauned/savage beast + closet KGB/Baseball), AshP94 skill-granting equipment conditional skills + coat-of-paint class-change refresh, AshP95 Eternity codpiece gem slots + EternityCodpiece modifiers + `have_skill` equipment merge, AshP96 crown/bjorn familiar desc sync (`Current Occupant` race parse), AshP97 Throne/Bjorn modifier accumulation in `CurrentModifiers` (worn crown/bjorn + enthroned/bjorned race), AshP98 `ItemAvailability`/`LimitModeGates` + `checkRing` itemAvailable wiring, AshP99 post-bird `SkillLearner.learnSkill` + `fetchSkills()` for Seek-a-Bird, AshP100 Eternity codpiece gem equip HTTP (`docodpiece` + choice 1588), AshP101 item access context (`buildCheckContext` storage/stash + Hermit clover gate + `AccessibleItemCount`), AshP102 battle `learnSkillFromResponse` (NEWSKILL parse + item consumption + leveled pref increment), AshP103 `AccessibleItemCount` depth (`PullableItems` LoL/Sea storage gates + worthless id 13 + Hat Trick hats + inactive familiar equipment), AshP104 `AccessibleItemCount` storage gates (`ThriftyRequest`/`StandardRequest` + `ItemRestriction.isAllowed` + ronin freepull split), AshP105 `StandardRequest.isAllowed` full parity (`TrendyRequest` + Thrifty evergreen + Quantum familiar + `ItemRestriction` wiring), AshP106 `FamiliarUsability`/`have_familiar` path gates + `RetrieveItemService` restricted-item early exit, AshP107 `in_terrarium` + Maximizer familiar switch usability + `RestrictionListRefresh` on retrieve, AshP108 `canUseFamiliars`/Quantum familiar gates + `use_familiar`/CLI/enthrone/bjornify usability + Maximizer enthrone/bjorn filtering, AshP109 CharacterState resource getters (`my_fury`/`my_pp`/`my_robot_*`/plumber stats) + `my_path_id` pathId fix + `EntityIntrospection` `name(entity)`/extended `to_int(entity)`, AshP110 charpane/combat class-resource sync (`ClassResourceCharpaneSync`/`ClassResourceCombatSync` + live `my_mask`/`my_paradoxicity`/`my_audience`/`my_discomomentum`/`telescope_upgrades`/`telescope_looked_high`), AshP111 campground garden sync (`GardenSync`/`CropType` + live `my_garden_type`), AshP112 closet+storage meat sync (`ClosetMeatSync`/`StorageMeatSync` + live `my_closet_meat`/`my_storage_meat`), AshP113 session meat tracking (`SessionMeatSync` + live `my_session_meat`), AshP114 desc(entity) description cache (`DescriptionCache` + cache-only item/effect/skill `desc()`), AshP115 storage pulls remaining sync (`pulls_remaining` + `ConcoctionDatabase` pulls state), AshP116 stills + mushroom plot ASH (`stills_available` + `StillsAvailability`/`StillSync`; `have_mushroom_plot` + `MushroomPlotSync`), AshP117 `craft_type(item)` concoction descriptions (`CraftTypeDescription` + `ConcoctionDatabase`), AshP118 `refresh_status`/`restore_hp`/`restore_mp` (`RecoveryManager.checkpointedRecoverHp/Mp` + api.php status refresh), AshP119 `mood_execute` + zodiac sign ASH (`MoodManager.checkpointedExecute` + `ZodiacSign` + `in_muscle_sign`/`in_mysticality_sign`/`in_moxie_sign`/`in_bad_moon` + `LimitModeGates.limitRecovery`), AshP120 consumption limit ASH (`ConsumptionEligibility` + `can_eat`/`can_drink` + `fullness_limit`/`inebriety_limit`/`spleen_limit`), AshP121 item property ASH (`is_tradeable`/`is_giftable`/`is_discardable`/`is_displayable`/`to_plural`/`get_power`/`weapon_hands`/`item_type`/`weapon_type` + `ItemDatabase`/`EquipmentDatabase` id lookups), AshP122 craft introspection ASH (`get_ingredients`/`creatable_amount` via `ConcoctionDatabase` + `CreatableAmount` + `AccessibleItemCount`), AshP123 shop probe + concoction pricing ASH (`is_npc_item`/`is_coinmaster_item`/`concoction_price` via `NpcStoreDatabase`/`CoinmasterDatabase`/`ConcoctionCreationCost`), AshP124 craft permission + turns + modifier-effective limits (`ConcoctionPermitted`/`CreatableTurns`/`creatable_turns` + path-base `ConsumptionEligibility` capacity), AshP125 recursive `creatable_turns` + 3-arg free-crafting credit (`CreatableTurns` ingredient tree), AshP126 free crafting ASH + SSPD/GRIMACITE gates (`FreeCraftingTurns` + `free_crafts`/`free_cooks`/`free_mixes`/`free_smiths`), AshP127 shop validate probes (`NpcPurchaseAccessibility`/`CoinmasterPurchaseProbe` + validate=true wiring), AshP128 ConcoctionPermitted v2 + consumption expand guards (`CharacterCapacity` + `can_expand_*`), AshP129 concoction method gates + `concoction_price(vykea)` (`ConcoctionMethodGates`/`ClanLoungeSync`), AshP130 NPC shop validate v2 (`NpcPurchaseAccessibility` hippy/chinatown/cyber/fwshop), AshP131 concoction method gates v2 (`ConcoctionMethodGates` TERMINAL/SPACEGATE/FANTASY_REALM/STILLSUIT/MAYAM/PHOTO_BOOTH/TAKERSPACE + `CampgroundItemSync`), AshP132 NPC shop validate v3 + `is_npc_item(id,validate)` (`NpcPurchaseAccessibility` generalstore/gnoll/mayoclinic/hippy depth), AshP133 concoction method gates v3 (`ConcoctionMethodGates` GNOME_PART/BURNING_LEAVES/WAX/NEWSPAPER/METEOROID/WOOL + fallout TERMINAL + `CampgroundItemSync` + `FamiliarUsability` craft callback), AshP134 NPC shop validate v4 (gift shop/unclep/tweedle/vault/generalstore trick-tot + effect/familiar validate hooks), AshP135 kitchen craft gates (`KitchenEquipmentGates` COOK/MIX/COOK_FANCY/MIX_FANCY + `CampgroundItemSync` kitchen equipment prefs), AshP136 NPC shop validate v5 (armory/bartender/bartlebys/vault3 + pirate ephemera + swashbuckling outfit gates), AshP137 STAFF/PHINEAS craft gates + kitchen fancy v2 (`FreeCraftingTurns` + Cocktail Magic), AshP138 `NpcShopSync` bartlebys/hippy HTML + white Citadel gate + `CoinmasterPurchaseAccessibility` + `is_coinmaster_item(id[,validate])`, AshP139 kitchen auto-buy v3 (`KitchenAutoBuy`/`BoxServantAvailability`), AshP140 coinmaster validate v7 (Jarlsberg/Swagger/Crimbo17), AshP141 craft gates v4 (`SmithingGates`/`KnollAvailability` SMITH/SSMITH/CLIPART/JEWELRY), AshP142 coinmaster validate v8 (Crimbo20/shore/replica/pixel), AshP143 ConcoctionPermitted v3 (TIKI/TORSO + ROLL/SEWER/MUSE/SUSE), AshP144 coinmaster validate v9 (starchart/sugarsheets/5dprinter), AshP145 ConcoctionPermitted v4 (`ConcoctionMethodAliases` compound tokens + gnomads beach/KoE + CLIPART `skillsRecalled`), AshP146 coinmaster validate v10 (bacon/arcade/dv/kiwi/fixodent + `FolderHolderAccessibility`), AshP147 shop visit sync v1 (`CoinmasterShopSync`/`NpcShopSync`), AshP148 validate v11 (bugbear/wildfire NPC + mystic psychosis pixels), AshP149 shop visit sync v2 (hippy/fwshop/mayoclinic/shore/dv + wildfire buy wiring), AshP150 validate v12 (sync-driven hippy/fwshop/shore/dv probes), AshP151 shop visit sync v3 (swagger/hiddentavern/jarl cosmic six-pack + store.php/peevpee.php hooks), AshP152 validate v13 (sync-driven hiddentavern/swagger/jarl probes), AshP153 shop visit sync v4 (mrreplica year + blackmarket MACGUFFIN unlock), AshP154 validate v14 (sync-driven mrreplica/blackmarket probes), AshP155 shop visit sync v5 (`FunALogUnlockPrefs` + piraterealm unlock prefs/`availableFunPoints`), AshP156 validate v15 (sync-driven PirateRealm Fun-a-Log probes), AshP157 shop visit sync v6 (`DripArmoryPrefs` + driparmory `drippyShieldUnlocked`), AshP158 validate v16 (sync-driven Drip Armory shield probes), AshP159 shop visit sync v7 (`TimeTowerSync` + 8 Chroner shops + `place=twitch` + Swagger `peevpee.php` visit fix), AshP160 validate v17 (`TimeTowerAccessibility` + toolbelt free-pull pref gate + sync-driven Allied HQ flak shield probes), AshP161 shop visit sync v8 (`TrapperSync`/`SpinMasterLatheSync`/`SeptEmberSync`/`JunkMagazineSync`), AshP162 validate v18 (`SpringBreakBeachAccessibility`/`TinkeringBenchGates` + SBB/vending/tinkering + trapper/lathe/sept/junk gates + broberry brogurt corpus), AshP163 shop visit sync v9 (`SleazeAirportSync`/`MerchTableSync`/`CoinmasterVisitInventory` + adventure/place sleaze detection + conmerch/swagger dynamic rows), AshP164 validate v19 (`SeptEmberSync.checkBalance` + Swagger non-season visit inventory + conmerch dynamic validate + toolbelt storage cache invalidation), AshP165 free-pull collections (`get_free_pulls`/`get_cached_free_pulls` + tower freepull cache invalidation), AshP166 validate v20 (`storage_amount` freepull sum + `TinkeringBenchPurchasedItem` + `FlowerTradeinAccessibility`), AshP167 shop visit sync v10 (`FlowerTradeinSync` dynamic rows + MerchTable Mr. A/Chroner token prefs + `CoinmasterVisitInventory.FLOWER_TRADEIN`), AshP168 validate v21 (flowertradein dynamic validate + `get_storage()` dual-cache refresh), AshP169 shop visit sync v11 (`Crimbo25SammySync` dynamic wad-cost rows + `CoinmasterVisitInventory.CRIMBO25_SAMMY`), AshP170 validate v22 (conmerch tattoo **9148** + Sammy Crymbocurrency wad-trade validate), AshP171 shop visit sync v12 (`Crimbo23ZoneSync` armory/bar/cafe/cottage/foundry control prefs from `place.php?whichplace=crimbo23`), AshP172 validate v23 (`Crimbo23ShopAccessibility` 8-shop elf/pirate control gates + Kelflar vest/Crimbuccaneer shirt armory validate), AshP173 shop visit sync v13 (`Crimbo23ShopSync` Elf MPC/piece-of-12/machine-parts/flotsam token prefs from crimbo23_* shop visits), AshP174 validate v24 (`CoinmasterSyncedTokenCount` MerchTable + Crimbo23 synced token pref validate + bar/cafe corpus), AshP175 shop visit sync v14 (`StandardRewardDatabase` + `ArmoryAndLeggerySync` visitShopRows + `ArmoryAndLeggeryShopRows` coinmaster registration), AshP176 validate v25 (standard-reward armory validate + moss mace corpus), AshP177 freepull bucket migration (`StorageBucketMigration` + `TimeTowerSync` toolbelt cache move), AshP178 validate v26 (Crimbo23 factory validate + trick coin corpus), AshP179 standard reward pulverization derive (`derivePulverization` + `EquipmentDatabase.getPulverization`), AshP180 pulverized currency acquire hooks + validate v27 (`StandardRewardCurrencySync` + `_armoryAndLeggeryCurrencyRefresh`), AshP181 armory refresh wiring (`StandardRewardRefresh` derive+rebuild on visit/currency gain), AshP182 validate v28 (visit-learn rows + currency refresh rebuild probes), AshP183 pulverize.txt static loader (`EquipmentDatabase.loadPulverizeFromText` + `PulverizeFlags.PULVERIZE_BITS`), AshP184 live `pulverize(item)` v1 (direct item-id aggregate at 1_000_000), AshP185 pulverize.txt upgrade/cluster rows (`deriveUpgrade`/`deriveCluster`), AshP186 live `pulverize(item)` v2 (`PulverizeAggregate` bitmask decode), AshP187 lazy `derivePulverization(id)` + `PulverizeImplications` + `isPulverizable`, AshP188 `initializePulverization()` login prefetch, AshP189 armory visit learn v2 (`StandardRewardDatabase.toData` session-log + `NpcStoreVisitOverlay` meat rows), AshP190 live `desc()` HTTP prefetch on cache miss, AshP191 generic shop inventory parse v1 (`ShopRowParser`/`ShopRowDatabase`/`ShopInventorySync` + shoprows.txt loader + visit learn session-log), AshP192 coinmaster validate v29 (visit-learned overlay probes), AshP193 shop inventory v2 (CONC concoction session-log + shops.txt `ShopType`/`craftingType` + MerchTable/Sammy `ShopRowParser` dedupe), AshP194 coinmaster validate v30 (visit-overlay authority via `hasVisitOverlay`), AshP195 shop inventory v3 (`FlowerTradeinSync` `ShopRowParser.parseSingleCostRows` dedupe + inverted Chroner/flower mapping + `Chroner (N)` stack counts), AshP196 coinmaster validate v31 (flowertradein visit-overlay authority + `hasTradeFlower` gate regression), AshP197 shop inventory v4 (TD2A `whichskill=` skill-row parse + `ItemStack.isSkill` + `ShopInventorySync` coinmaster learn + `ShopRowDatabase.parseItemOrMeatOrSkill`), AshP198 coinmaster validate v32 (visit-overlay skill probes + `containsBuySkill`/`visitInventorySkillAvailable` + `hasSkill` gate + empty-overlay authority fix), AshP199 shop inventory v5 (legacy buy/sell row split + `ShopRowDatabase.registerShop` + `parseShopNameFromHtml` + legacy buy/sell session-log format), AshP200 coinmaster validate v33 (visit-overlay sell probes + `containsSellItem`/`visitInventorySellAvailable` + inventory validate), AshP201 shop inventory v6 (`SkillDefinitionDatabase.registerFromShopVisit` + TD3A skill name parse + `ShopRowDatabase.promoteShopType` + visit-inferred shop types), AshP202 coinmaster validate v34 (dynamic skill shop visit overlay + optional desc_skill prefetch + `corpus_dynamicSkillShopVisit_live`), AshP203 shop inventory v7 (`CoinmasterData.visitShopRows`/`isDisabled` + `ShopRowDatabase.logVisits` + `ShopInventorySync` hook/disabled/newStyle + armory/flowertradein/crimbo25_sammy `applyVisitShopRows` dedupe), AshP204 coinmaster validate v35 (disabled visit-overlay authority + hook-driven dynamic shop validate + `corpus_disabledCoinmasterVisitOverlay_live`), AshP205 shop inventory v8 (`CoinmasterData.visitShop` + `MerchTableSync` conmerch hooks + disabled legacy classify), AshP206 coinmaster validate v36 (conmerch hook-driven visit overlay validate + `corpus_conmerchVisitOverlayValidate_live`), AshP207 shop inventory v9 (`visitShop` hook batch 2: trapper/driparmory/september/lathe/junkmagazine), AshP208 coinmaster validate v37 (hook-driven drip shield + trapper pref gates), AshP209 shop inventory v10 (`visitShop` hook batch 3: bacon/arcade/kiwi/mystic/shore/5dprinter), AshP210 coinmaster validate v38 (hook-driven mystic psychosis pixel + shore toaster gates), AshP211 shop inventory v11 (`visitShop` hook batch 4: mrreplica/blackmarket/piraterealm/chroner/crimbo23/swagger + final `CoinmasterShopSync` pref-sync dedupe), AshP212 coinmaster validate v39 (hook-driven mrreplica/blackmarket/piraterealm/swagger/alliedhq gates), AshP213 shoprows pref write-back + public `is_coinmaster_skill` ASH + remove no-op `CoinmasterShopSync.apply`, AshP214 coinmaster validate v40 (`is_coinmaster_skill` + learnedShopRows restore probes), AshP215 shop hook corpus v41 (AshP205–214 visit-hook ASH validate regression), AshP216 shop cleanup/validate v42 (delete dead `CoinmasterShopSync.apply` + learnedShopRows visit-overlay restore), modifier lookups via `modifiers/*`; roadmap: `2026-06-11-ash-script-full-parity.md`).
- Run unit tests with `.\gradlew.bat :shared:jvmTest`; verify Android builds with `.\gradlew.bat :androidApp:assembleDebug`.
- On Windows PowerShell, chain shell commands with `;`, not `&&`.
- Incremental parity phases use Quest/CLI/Maximizer tracks; Phases 53–210 advanced Tier 1 (AshP19–AshP115 entity/modifier/monster/combat libraries including MONSTER/LOCATION/PATH brackets, combat adjustment, drop/XP/initiative/meat/item drops, monster combat stats/element/`jump_chance`, location `jump_chance`, hit/miss APIs, `all_monsters_with_id`, expression Init/Atk/Def/HP, numeric + expression Scale/Cap/Floor, Exp/`base_mainstat_exp`, `REDUCE_ENEMY_DEFENSE`, Phys/Elem resistance brackets, `MLMult:` effective ML, ML phys-res boost, `$monster[raw_*]` / `$monster[base_*]` effective, `monster_initiative` initPenalty, `$monster[poison|group|manuel_name|wiki_name]`, `$monster[attack_elements]` multi-EA, `$monster[sub_types|images]`, `$monster[random_modifiers|attributes]` (runtime via MonsterStatusTracker + EncounterModifierPipeline), RandomModifierStats lazy apply on last_monster, monster beeosity (Beecore) on effective stats, Intergnat demonName12 sync from fight HTML, equipment beeosity (Maximizer/BHY), Summoning Chamber `summon` CLI, quest-log demonName sync, Yeg demonName13 cargo scrap sync, demonName14 segment tracking + `demons solve14` CLI, Allied Radio HTTP + `alliedradio` CLI, QUEST_LOG modifier-expression consequences (`royalty` pref via `ConsequenceActionResolver`), cargo-cult picked-pocket sync + `cargo` CLI + `picked_pockets`/`picked_scraps` ASH, PocketDatabase + cargo count/list/typed-pick + `pocket_monster`/`available_pocket`/`pick_pocket` ASH, cargo pocket introspection + `registerPocketFight`, DESC_ITEM consequence expressions + `ItemDescriptionConsequenceSync` + `roman()`/`monstername`, DESC_ITEM `=mods` enchantment parse + `ModifierDatabase.overrideModifier` + `DynamicItemModifierSync`, DESC_EFFECT consequence expressions + `=mods` + `EffectDescriptionConsequenceSync` + effect cached pref restore, DESC_SKILL consequence expressions + `SkillDescriptionConsequenceSync` + desc_skill self=true hook, COMBAT_SKILL consequence expressions + `CombatSkillConsequenceSync` + fight dropdown hook, MONSTER disambiguation consequences + `MonsterConsequenceSync` + `disambiguateMonster`, dynamic modifier checkMods v1 + `checkDynamicModifiers` on login/refresh, consequence `updateOneDesc` rotation prefetch, extended checkMods v2, login desc checks v3 (crown/bjorn/bird-of-day/entauntauned/savage beast + closet KGB/Baseball), skill-granting equipment conditional skills + coat-of-paint class-change refresh, Eternity codpiece `eternitycod` API + CODPIECE1–5 slots + gem modifiers + codpiece conditional skills + `have_skill` equipment merge, crown/bjorn familiar desc sync (`Current Occupant` race parse), Throne/Bjorn modifier accumulation in `CurrentModifiers`, `ItemAvailability`/`LimitModeGates` itemAvailable gates, post-bird `SkillLearner.learnSkill` + `fetchSkills()`, Eternity codpiece gem equip HTTP (`EquipmentRequest` docodpiece + choice 1588), `buildCheckContext` storage/stash IDs + Hermit clover coinmaster gate + `AccessibleItemCount` for `available_amount`/retrieve check-only, battle `learnSkillFromResponse` (NEWSKILL parse + item consumption + leveled pref increment), `AccessibleItemCount` depth (LoL/Sea storage gates + worthless id 13 + Hat Trick + inactive familiar equipment), `AccessibleItemCount` storage gates (Thrifty/Standard restriction + ronin freepull split), `StandardRequest.isAllowed` full parity (Trendy/Thrifty evergreen/Quantum familiar), `FamiliarUsability`/`have_familiar` path gates + retrieve restricted-item early exit, `in_terrarium` ownership-only ASH + Maximizer familiar switch usability + `RestrictionListRefresh` on retrieve, `canUseFamiliars`/Quantum gates + `use_familiar`/CLI/enthrone/bjornify usability + Maximizer enthrone/bjorn filtering, AshP109 CharacterState resource getters + `my_path_id` + `EntityIntrospection` name/extended to_int, AshP110 charpane/combat class-resource sync, AshP111 campground garden sync (`GardenSync` + live `my_garden_type`), AshP112 closet+storage meat sync + AshP113 session meat tracking (`SessionMeatSync` + live `my_session_meat`), AshP114 desc(entity) cache-only description lookups (`DescriptionCache` + live `desc()` for item/effect/skill), AshP115 storage pulls remaining sync (`pulls_remaining` + `StorageMeatSync` pullsleft parse), AshP116 stills + mushroom plot ASH (`stills_available` gate logic + `StillSync`; `have_mushroom_plot` + `MushroomPlotSync`), AshP117 `craft_type(item)` concoction method descriptions (`CraftTypeDescription`), AshP118 `refresh_status`/`restore_hp`/`restore_mp` (`RecoveryManager.checkpointedRecoverHp/Mp`), AshP119 `mood_execute` + zodiac sign booleans (`ZodiacSign` + `MoodManager.checkpointedExecute`), AshP120 consumption limit ASH (`ConsumptionEligibility` + `can_eat`/`can_drink` + capacity limits), AshP121 item property ASH (`ItemDatabase`/`EquipmentDatabase` + trade/gift/discard/display/plural/power/hands/type), AshP122 craft introspection ASH (`get_ingredients`/`creatable_amount` + `CreatableAmount`), AshP123 shop probes + concoction pricing (`is_npc_item`/`is_coinmaster_item`/`concoction_price`), AshP124 craft permission gating + `creatable_turns` + path-base modifier-effective consumption limits (`ConcoctionPermitted`/`CreatableTurns`/`ConcoctionCreationCost`), `$monster[fact|fact_type]`, `$monster[min_sprinkles|max_sprinkles]`, CLI long-tail including `journey`/`witchess`/`volcano`, charpane sync for Ed/VYKEA/pasta thrall/class resources, AshP125 recursive `creatable_turns` + AshP126 free crafting ASH, AshP127 shop validate probes + AshP128 ConcoctionPermitted depth + AshP129 concoction method gates + AshP130 NPC shop validate v2 + AshP131 concoction method gates v2 + AshP132 NPC shop validate v3 + AshP133 concoction method gates v3 + AshP134 NPC shop validate v4 + AshP135 kitchen craft gates + AshP136 NPC shop validate v5 + AshP137 STAFF/PHINEAS/kitchen fancy v2 + AshP138 NpcShopSync/coinmaster validate v6 + AshP139 kitchen auto-buy v3 (`KitchenAutoBuy`/`BoxServantAvailability` willBuyTool/willBuyServant + COOK/MIX_FANCY v3) + AshP140 coinmaster validate v7 (Jarlsberg/Swagger/Crimbo17 gates + `is_npc_item(id)` 1-arg) + AshP141 craft gates v4 (SMITH/SSMITH/CLIPART/JEWELRY + `KnollAvailability`/`SmithingGates`) + AshP142 coinmaster validate v8 (Crimbo20/shore/replica/pixel + `DesertBeachAccessibility`/`ReplicaMrStoreAccessibility`) + AshP143 ConcoctionPermitted v3 (TIKI/TorsoAwareness + explicit ROLL/SEWER/MUSE/SUSE) + AshP145 ConcoctionPermitted v4 (`ConcoctionMethodAliases` compound tokens + gnomads beach/KoE + CLIPART `skillsRecalled`) + AshP146 coinmaster validate v10 (bacon/arcade/dv/kiwi/fixodent + `FolderHolderAccessibility`) + AshP147 shop visit sync v1 + AshP148 validate v11 (bugbear/wildfire NPC + mystic psychosis pixels) + AshP149 shop visit sync v2 (hippy/fwshop/mayoclinic/shore/dv visit+purchase pref sync + `NpcBuyRequest` wildfire buy wiring) + AshP150 validate v12 (sync-driven hippy peach/fwshop hat/shore toaster/dv flask probes) + AshP151 shop visit sync v3 (`SwaggerShopSync` + jarl cosmic six-pack purchase hook + `NpcShopSync` hiddentavern unlock + `store.php`/`peevpee.php` visit hooks) + AshP152 validate v13 (sync-driven hiddentavern/swagger/jarl probes) + AshP153 shop visit sync v4 (`CoinmasterShopSync` mrreplica year + blackmarket MACGUFFIN unlock) + AshP154 validate v14 (sync-driven mrreplica/blackmarket probes) + AshP155 shop visit sync v5 (`FunALogUnlockPrefs` + piraterealm unlock prefs/`availableFunPoints`) + AshP156 validate v15 (sync-driven PirateRealm Fun-a-Log probes) + AshP157 shop visit sync v6 (`DripArmoryPrefs` + driparmory `drippyShieldUnlocked`) + AshP158 validate v16 (sync-driven Drip Armory shield probes) + AshP159 shop visit sync v7 (`TimeTowerSync`/`TimeTowerAccessibility` + Chroner shops + `place=twitch` + Swagger visit fix) + AshP160 validate v17 (toolbelt free-pull pref gate + Allied HQ flak shield probes) + AshP161 shop visit sync v8 (`TrapperSync`/`SpinMasterLatheSync`/`SeptEmberSync`/`JunkMagazineSync`) + AshP162 validate v18 + AshP165 free-pull collections + AshP166 validate v20 (`get_free_pulls`/`get_cached_free_pulls` + `storage_amount` freepull sum + `TinkeringBenchPurchasedItem` + `FlowerTradeinAccessibility`) + AshP167 shop visit sync v10 (`FlowerTradeinSync` + MerchTable token prefs) + AshP168 validate v21 (flowertradein dynamic validate + `get_storage()` dual-cache refresh) + AshP169 shop visit sync v11 (`Crimbo25SammySync`) + AshP170 validate v22 (conmerch tattoo + Sammy wad-trade validate) + AshP181 armory refresh + AshP182 validate v28, AshP183 pulverize.txt loader + AshP184 `pulverize(item)` v1 + AshP185 upgrade/cluster loader + AshP186 `pulverize(item)` v2, AshP187 lazy pulverize derive + AshP188 login prefetch + AshP189 armory visit learn v2 + AshP190 desc() HTTP prefetch + AshP191 shop inventory parse + AshP192 validate v29, AshP193 shop inventory v2 + AshP194 validate v30 + AshP195 shop inventory v3 + AshP196 validate v31 + AshP197 shop inventory v4 + AshP198 validate v32 + AshP199 shop inventory v5 + AshP200 validate v33 + AshP201 shop inventory v6 + AshP202 validate v34 + AshP203 shop inventory v7 + AshP204 validate v35 + AshP205 shop inventory v8 + AshP206 validate v36 + AshP207 shop inventory v9 + AshP208 validate v37, AshP209 shop inventory v10 + AshP210 validate v38 + AshP211 shop inventory v11 (`ReplicaMrStoreSync`/`BlackMarketShopSync`/`PirateRealmShopSync`/`ChronerShopSync`/`Crimbo23ShopSync.applyVisitShop`/`SwaggerShopSync`) + AshP212 validate v39, AshP213 shoprows pref write-back + `is_coinmaster_skill` ASH + AshP214 validate v40, AshP215 shop hook corpus v41 + AshP216 cleanup/validate v42, tower/hedge maze/PirateRealm sync); `GameRuntimeLibrary.REVISION` uses `phaseNN` format (currently `phase210`); update `docs/parity-audit.md` after each phase.
- Quest state sync combines QuestAdvanceRules, QuestChoiceRules, QuestFightRules, QuestItemRules, QuestLogSync, QuestSpecialSync, and dedicated `quest/*Sync.kt` visit parsers (e.g. TowerSync, TelescopeSync, PirateRealmSync); Ed servants in `servant/EdServantManager.kt` with `EdServantChoiceSync`/`EdServantCharpaneSync`; VYKEA companion in `vykea/VykeaCompanionManager.kt` with `VykeaCharpaneSync`; pasta thrall in `thrall/PastaThrallManager.kt` with `PastaThrallCharpaneSync`; class/path resources in `character/ClassResourceCharpaneSync.kt`/`ClassResourceCombatSync.kt`; campground garden in `campground/GardenSync.kt`; still shop + mushroom plot in `concoction/StillSync.kt`/`campground/MushroomPlotSync.kt`; closet/storage/session meat in `inventory/ClosetMeatSync.kt`/`StorageMeatSync.kt`/`SessionMeatSync.kt`; entity description cache + live HTTP prefetch in `data/DescriptionCache.kt`/`EntityIntrospection.kt`; armory visit learn in `shop/ArmoryAndLeggerySync.kt`/`NpcStoreVisitOverlay.kt`; generic shop inventory parse in `shop/ShopRowParser.kt`/`ShopRowDatabase.kt`/`ShopInventorySync.kt`; bundled data loaders: `journeyman.txt` via `data/JourneymanDatabase.kt`, `witchess_solutions.txt` via `data/WitchessSolutionDatabase.kt`/`session/WitchessManager.kt`, `volcanomaze.txt` via `data/VolcanoMazeDatabase.kt`/`session/VolcanoMazeManager.kt`, `cultshorts.txt` via `data/PocketDatabase.kt`/`data/CultShortsDatabase.kt`/`session/YegDemonNameSync.kt`; tower automation in `TowerDoorRunner.kt`/`TowerDoorConfig.kt`; hedge maze in `HedgeMazeRunner.kt`/`HedgeMazeConfig.kt`.
- ASH runtime functions register via `regFn()` in `GameRuntimeLibrary.*.kt` under `ash/`; AshScope uses first-match overload resolution — remove AshP11 stubs and wire live lookups in AshP12–P18 `resolveModifier*` helpers when adding AshP19+ behavioral batches.
- `AshCompatibilityCorpusTest` is the behavioral ASH regression corpus; JavaScript runtime and relay server are explicit parity non-goals.
- Git default branch is `master`; avoid stale `.worktrees/` for active development; exclude `.cursor/` hook state from commits unless requested.

