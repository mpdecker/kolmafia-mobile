## Learned User Preferences

- Do not edit attached plan files during implementation; treat them as read-only reference.
- Do not recreate plan todos; mark existing ones in_progress and complete all before stopping.
- Do not git commit unless explicitly requested; preserve unrelated user changes and existing uncommitted work, and do not reset or discard them.
- Use the `gh` CLI for GitHub pull requests when asked.
- After phase implementation, update `docs/parity-audit.md` with metrics and a history entry; short "parity audit" means recount desktop-vs-mobile metrics, reprioritize Top Priorities from remaining gaps, and return explicitly deferred items to the task queue.
- Phase planning and gap-closing work should follow `docs/parity-audit.md` Top Priorities.
- Short "pr" means commit current work and open or update a GitHub PR via `gh`.
- Short "push" means git push the current branch to remote.
- Diff-tab "commit-and-push" means commit current work and push to remote.
- Prefer massive multi-phase gap-close batches (on the order of ~60 phases) over small single-phase slices when planning or when the user says scope is too small / should be massive; "next pass", "next", "next phase", "next major phase", "major huge next phase", "next major giant phase", "next large multi-phase slice", "next large multiphase slice", "next such multiphase pass", "next multiphase pass", "next multiphase slice", "next major multiphase gap closing pass", "next multiphase gap closing pass", "equally sized gap closing pass", "next comparably sized batch towards parity", "next such gap closing phase", "full parity push", "next phaser", "NEXT PHADE MEGA", or "proceed with further multi-phase slices" means continue the next incremental parity phase or multi-phase batch from `docs/parity-audit.md` Top Priorities; "next major phase beyond this scope" means switch to the next higher Top Priorities track rather than another slice of the current one; short "next pvp major slice" or "next pvp parity pass" means continue the next incremental PvP batch from Top Priority #11.
- Short "continue" means resume in-progress parity work or proceed with Top Priorities when between tasks.
- When committing parity work, stage all relevant changed files across accumulated phases in the working tree (not just the latest phase); exclude unrelated, `.cursor/`, `.worktrees/`, `tools/`, and line-ending-only changes.

## Learned Workspace Facts

- Kotlin Multiplatform KoLmafia mobile port; core logic lives in `shared/src/commonMain`; modules include `shared`, `androidApp`, and platform targets.
- Desktop KoLmafia Java reference source is at `C:\Development\kolmafia\kolmafia\src\`; ported data files live in `shared/src/commonMain/composeResources/files/data/`.
- `docs/parity-audit.md` is the authoritative desktop-vs-mobile parity document.
- Phases 849?876 add Quest ChoiceControl Tracks D?F plus `session/GreyYouManager.kt`; choice wiring is centralized through `QuestChoiceRules`, and Grey You charsheet/fight hooks live in `GameRuntimeLibrary`/`AdventureManager`.
- Phase plans live under `docs/superpowers/plans/`; ASH batches in `GameRuntimeLibrary.AshP*Batch.kt` ? AshP8?P18 registration floor, AshP19?AshP269 live behavioral batches (entity `to_*` resolvers, `$entity[field]` bracket reads via `*EntityFields.kt` including MONSTER/LOCATION/PATH/ITEM fields + `$monster[base_mainstat_exp]` + `$monster[*_resistance]` + `$monster[raw_*]` + `$monster[base_*]` effective + `$monster[poison|group|manuel_name|wiki_name]` + `$monster[attack_elements]` + `$monster[sub_types|images]` + `$monster[random_modifiers|attributes]` + `$monster[fact|fact_type]` + `$monster[min_sprinkles|max_sprinkles]`, AshP39 `CombatAdjustment`, AshP40 drop/XP/initiative modifiers, AshP41?43 monster combat stats/element/`jump_chance`, AshP44 location `jump_chance`, AshP45 `meat_drop`/`item_drops`, AshP46 hit/miss combat APIs, AshP47 `all_monsters_with_id`, AshP48 jump Init polish, AshP49?52 expression Init/Atk/Def/HP eval, AshP53?54 numeric/expression Scale/Cap/Floor + `equipped()`, AshP55 Exp / `base_mainstat_exp`, AshP56 `REDUCE_ENEMY_DEFENSE`, AshP57 Phys/Elem resistance brackets, AshP58 `MLMult:` effective ML, AshP59 ML phys-res boost, AshP60 raw_* brackets, AshP61 base_* effective brackets, AshP62 `monster_initiative` initPenalty, AshP63 poison/group/manuel/wiki metadata, AshP64 multi-EA parse + attack_elements bracket, AshP65 sub_types/images brackets, AshP66 random_modifiers/attributes brackets, AshP67 fact/fact_type + FactDatabase, AshP68 min/max sprinkles + SprinkleMin/Max parse, AshP69 runtime random_modifiers + MonsterStatusTracker/OCRS parser, AshP70 RandomModifierStats lazy apply on last_monster, AshP71 EncounterModifierPipeline + leet name resolution, AshP72 monster beeosity (Beecore) on effective stats, AshP73 IntergnatDemonNameSync + demonName12 pref, AshP74 equipment beeosity (Maximizer/BHY), AshP75 SummoningChamberManager + summon CLI, AshP76 QuestLogConsequenceSync + demonName prefs, AshP77 YegDemonNameSync + cultshorts scrap syllables + demonName13 pref, AshP78 DemonInCombatNameSync + DemonName14Manager + demons CLI, AshP79 AlliedRadioManager + alliedradio CLI, AshP80 ConsequenceActionResolver + QUEST_LOG `SetExpressionValue` (`royalty` pref), AshP81 CargoPocketSync + `cargo` CLI + `picked_pockets`/`picked_scraps` ASH, AshP82 PocketDatabase + cargo count/list/typed-pick CLI + `pocket_monster`/`available_pocket`/`pick_pocket` ASH, AshP83 cargo pocket introspection (`potential_pockets`, `pocket_*`, list helpers) + `registerPocketFight`, AshP84 DESC_ITEM consequence expressions + `ItemDescriptionConsequenceSync` + `roman()`/`monstername`, AshP85 DESC_ITEM `=mods` + `ModifierEnchantmentParser`/`ItemEnchantmentParser` + `ModifierDatabase.overrideModifier` + `DynamicItemModifierSync`, AshP86 DESC_EFFECT consequence expressions + `=mods` + `EffectEnchantmentParser`/`EffectDescriptionConsequenceSync` + effect cached pref restore, AshP87 DESC_SKILL consequence expressions + `SkillDescriptionConsequenceSync` + desc_skill self=true hook, AshP88 COMBAT_SKILL consequence expressions + `CombatSkillConsequenceSync` + fight dropdown hook + `ExpressionContext` in `ConsequenceActionResolver`, AshP89 MONSTER disambiguation consequences + `MonsterConsequenceSync` + `ReturnReplacement` quoted actions, AshP90 dynamic modifier checkMods v1 + `checkDynamicModifiers` on login/refresh + KGB/Baseball owned desc visits, AshP91 consequence `updateOneDesc` rotation + `DescriptionConsequenceRegistry` + `DescriptionConsequenceSync`, AshP92 extended checkMods v2 (closet accessibility + saber/umbrella/wine/crimbo/ring/G9/zoot/heartstone + dart holster/mimic egg), AshP93 login desc checks v3 (crown/bjorn/bird-of-day/entauntauned/savage beast + closet KGB/Baseball), AshP94 skill-granting equipment conditional skills + coat-of-paint class-change refresh, AshP95 Eternity codpiece gem slots + EternityCodpiece modifiers + `have_skill` equipment merge, AshP96 crown/bjorn familiar desc sync (`Current Occupant` race parse), AshP97 Throne/Bjorn modifier accumulation in `CurrentModifiers` (worn crown/bjorn + enthroned/bjorned race), AshP98 `ItemAvailability`/`LimitModeGates` + `checkRing` itemAvailable wiring, AshP99 post-bird `SkillLearner.learnSkill` + `fetchSkills()` for Seek-a-Bird, AshP100 Eternity codpiece gem equip HTTP (`docodpiece` + choice 1588), AshP101 item access context (`buildCheckContext` storage/stash + Hermit clover gate + `AccessibleItemCount`), AshP102 battle `learnSkillFromResponse` (NEWSKILL parse + item consumption + leveled pref increment), AshP103 `AccessibleItemCount` depth (`PullableItems` LoL/Sea storage gates + worthless id 13 + Hat Trick hats + inactive familiar equipment), AshP104 `AccessibleItemCount` storage gates (`ThriftyRequest`/`StandardRequest` + `ItemRestriction.isAllowed` + ronin freepull split), AshP105 `StandardRequest.isAllowed` full parity (`TrendyRequest` + Thrifty evergreen + Quantum familiar + `ItemRestriction` wiring), AshP106 `FamiliarUsability`/`have_familiar` path gates + `RetrieveItemService` restricted-item early exit, AshP107 `in_terrarium` + Maximizer familiar switch usability + `RestrictionListRefresh` on retrieve, AshP108 `canUseFamiliars`/Quantum familiar gates + `use_familiar`/CLI/enthrone/bjornify usability + Maximizer enthrone/bjorn filtering, AshP109 CharacterState resource getters (`my_fury`/`my_pp`/`my_robot_*`/plumber stats) + `my_path_id` pathId fix + `EntityIntrospection` `name(entity)`/extended `to_int(entity)`, AshP110 charpane/combat class-resource sync (`ClassResourceCharpaneSync`/`ClassResourceCombatSync` + live `my_mask`/`my_paradoxicity`/`my_audience`/`my_discomomentum`/`telescope_upgrades`/`telescope_looked_high`), AshP111 campground garden sync (`GardenSync`/`CropType` + live `my_garden_type`), AshP112 closet+storage meat sync (`ClosetMeatSync`/`StorageMeatSync` + live `my_closet_meat`/`my_storage_meat`), AshP113 session meat tracking (`SessionMeatSync` + live `my_session_meat`), AshP114 desc(entity) description cache (`DescriptionCache` + cache-only item/effect/skill `desc()`), AshP115 storage pulls remaining sync (`pulls_remaining` + `ConcoctionDatabase` pulls state), AshP116 stills + mushroom plot ASH (`stills_available` + `StillsAvailability`/`StillSync`; `have_mushroom_plot` + `MushroomPlotSync`), AshP117 `craft_type(item)` concoction descriptions (`CraftTypeDescription` + `ConcoctionDatabase`), AshP118 `refresh_status`/`restore_hp`/`restore_mp` (`RecoveryManager.checkpointedRecoverHp/Mp` + api.php status refresh), AshP119 `mood_execute` + zodiac sign ASH (`MoodManager.checkpointedExecute` + `ZodiacSign` + `in_muscle_sign`/`in_mysticality_sign`/`in_moxie_sign`/`in_bad_moon` + `LimitModeGates.limitRecovery`), AshP120 consumption limit ASH (`ConsumptionEligibility` + `can_eat`/`can_drink` + `fullness_limit`/`inebriety_limit`/`spleen_limit`), AshP121 item property ASH (`is_tradeable`/`is_giftable`/`is_discardable`/`is_displayable`/`to_plural`/`get_power`/`weapon_hands`/`item_type`/`weapon_type` + `ItemDatabase`/`EquipmentDatabase` id lookups), AshP122 craft introspection ASH (`get_ingredients`/`creatable_amount` via `ConcoctionDatabase` + `CreatableAmount` + `AccessibleItemCount`), AshP123 shop probe + concoction pricing ASH (`is_npc_item`/`is_coinmaster_item`/`concoction_price` via `NpcStoreDatabase`/`CoinmasterDatabase`/`ConcoctionCreationCost`), AshP124 craft permission + turns + modifier-effective limits (`ConcoctionPermitted`/`CreatableTurns`/`creatable_turns` + path-base `ConsumptionEligibility` capacity), AshP125 recursive `creatable_turns` + 3-arg free-crafting credit (`CreatableTurns` ingredient tree), AshP126 free crafting ASH + SSPD/GRIMACITE gates (`FreeCraftingTurns` + `free_crafts`/`free_cooks`/`free_mixes`/`free_smiths`), AshP127 shop validate probes (`NpcPurchaseAccessibility`/`CoinmasterPurchaseProbe` + validate=true wiring), AshP128 ConcoctionPermitted v2 + consumption expand guards (`CharacterCapacity` + `can_expand_*`), AshP129 concoction method gates + `concoction_price(vykea)` (`ConcoctionMethodGates`/`ClanLoungeSync`), AshP130 NPC shop validate v2 (`NpcPurchaseAccessibility` hippy/chinatown/cyber/fwshop), AshP131 concoction method gates v2 (`ConcoctionMethodGates` TERMINAL/SPACEGATE/FANTASY_REALM/STILLSUIT/MAYAM/PHOTO_BOOTH/TAKERSPACE + `CampgroundItemSync`), AshP132 NPC shop validate v3 + `is_npc_item(id,validate)` (`NpcPurchaseAccessibility` generalstore/gnoll/mayoclinic/hippy depth), AshP133 concoction method gates v3 (`ConcoctionMethodGates` GNOME_PART/BURNING_LEAVES/WAX/NEWSPAPER/METEOROID/WOOL + fallout TERMINAL + `CampgroundItemSync` + `FamiliarUsability` craft callback), AshP134 NPC shop validate v4 (gift shop/unclep/tweedle/vault/generalstore trick-tot + effect/familiar validate hooks), AshP135 kitchen craft gates (`KitchenEquipmentGates` COOK/MIX/COOK_FANCY/MIX_FANCY + `CampgroundItemSync` kitchen equipment prefs), AshP136 NPC shop validate v5 (armory/bartender/bartlebys/vault3 + pirate ephemera + swashbuckling outfit gates), AshP137 STAFF/PHINEAS craft gates + kitchen fancy v2 (`FreeCraftingTurns` + Cocktail Magic), AshP138 `NpcShopSync` bartlebys/hippy HTML + white Citadel gate + `CoinmasterPurchaseAccessibility` + `is_coinmaster_item(id[,validate])`, AshP139 kitchen auto-buy v3 (`KitchenAutoBuy`/`BoxServantAvailability`), AshP140 coinmaster validate v7 (Jarlsberg/Swagger/Crimbo17), AshP141 craft gates v4 (`SmithingGates`/`KnollAvailability` SMITH/SSMITH/CLIPART/JEWELRY), AshP142 coinmaster validate v8 (Crimbo20/shore/replica/pixel), AshP143 ConcoctionPermitted v3 (TIKI/TORSO + ROLL/SEWER/MUSE/SUSE), AshP144 coinmaster validate v9 (starchart/sugarsheets/5dprinter), AshP145 ConcoctionPermitted v4 (`ConcoctionMethodAliases` compound tokens + gnomads beach/KoE + CLIPART `skillsRecalled`), AshP146 coinmaster validate v10 (bacon/arcade/dv/kiwi/fixodent + `FolderHolderAccessibility`), AshP147 shop visit sync v1 (`CoinmasterShopSync`/`NpcShopSync`), AshP148 validate v11 (bugbear/wildfire NPC + mystic psychosis pixels), AshP149 shop visit sync v2 (hippy/fwshop/mayoclinic/shore/dv + wildfire buy wiring), AshP150 validate v12 (sync-driven hippy/fwshop/shore/dv probes), AshP151 shop visit sync v3 (swagger/hiddentavern/jarl cosmic six-pack + store.php/peevpee.php hooks), AshP152 validate v13 (sync-driven hiddentavern/swagger/jarl probes), AshP153 shop visit sync v4 (mrreplica year + blackmarket MACGUFFIN unlock), AshP154 validate v14 (sync-driven mrreplica/blackmarket probes), AshP155 shop visit sync v5 (`FunALogUnlockPrefs` + piraterealm unlock prefs/`availableFunPoints`), AshP156 validate v15 (sync-driven PirateRealm Fun-a-Log probes), AshP157 shop visit sync v6 (`DripArmoryPrefs` + driparmory `drippyShieldUnlocked`), AshP158 validate v16 (sync-driven Drip Armory shield probes), AshP159 shop visit sync v7 (`TimeTowerSync` + 8 Chroner shops + `place=twitch` + Swagger `peevpee.php` visit fix), AshP160 validate v17 (`TimeTowerAccessibility` + toolbelt free-pull pref gate + sync-driven Allied HQ flak shield probes), AshP161 shop visit sync v8 (`TrapperSync`/`SpinMasterLatheSync`/`SeptEmberSync`/`JunkMagazineSync`), AshP162 validate v18 (`SpringBreakBeachAccessibility`/`TinkeringBenchGates` + SBB/vending/tinkering + trapper/lathe/sept/junk gates + broberry brogurt corpus), AshP163 shop visit sync v9 (`SleazeAirportSync`/`MerchTableSync`/`CoinmasterVisitInventory` + adventure/place sleaze detection + conmerch/swagger dynamic rows), AshP164 validate v19 (`SeptEmberSync.checkBalance` + Swagger non-season visit inventory + conmerch dynamic validate + toolbelt storage cache invalidation), AshP165 free-pull collections (`get_free_pulls`/`get_cached_free_pulls` + tower freepull cache invalidation), AshP166 validate v20 (`storage_amount` freepull sum + `TinkeringBenchPurchasedItem` + `FlowerTradeinAccessibility`), AshP167 shop visit sync v10 (`FlowerTradeinSync` dynamic rows + MerchTable Mr. A/Chroner token prefs + `CoinmasterVisitInventory.FLOWER_TRADEIN`), AshP168 validate v21 (flowertradein dynamic validate + `get_storage()` dual-cache refresh), AshP169 shop visit sync v11 (`Crimbo25SammySync` dynamic wad-cost rows + `CoinmasterVisitInventory.CRIMBO25_SAMMY`), AshP170 validate v22 (conmerch tattoo **9148** + Sammy Crymbocurrency wad-trade validate), AshP171 shop visit sync v12 (`Crimbo23ZoneSync` armory/bar/cafe/cottage/foundry control prefs from `place.php?whichplace=crimbo23`), AshP172 validate v23 (`Crimbo23ShopAccessibility` 8-shop elf/pirate control gates + Kelflar vest/Crimbuccaneer shirt armory validate), AshP173 shop visit sync v13 (`Crimbo23ShopSync` Elf MPC/piece-of-12/machine-parts/flotsam token prefs from crimbo23_* shop visits), AshP174 validate v24 (`CoinmasterSyncedTokenCount` MerchTable + Crimbo23 synced token pref validate + bar/cafe corpus), AshP175 shop visit sync v14 (`StandardRewardDatabase` + `ArmoryAndLeggerySync` visitShopRows + `ArmoryAndLeggeryShopRows` coinmaster registration), AshP176 validate v25 (standard-reward armory validate + moss mace corpus), AshP177 freepull bucket migration (`StorageBucketMigration` + `TimeTowerSync` toolbelt cache move), AshP178 validate v26 (Crimbo23 factory validate + trick coin corpus), AshP179 standard reward pulverization derive (`derivePulverization` + `EquipmentDatabase.getPulverization`), AshP180 pulverized currency acquire hooks + validate v27 (`StandardRewardCurrencySync` + `_armoryAndLeggeryCurrencyRefresh`), AshP181 armory refresh wiring (`StandardRewardRefresh` derive+rebuild on visit/currency gain), AshP182 validate v28 (visit-learn rows + currency refresh rebuild probes), AshP183 pulverize.txt static loader (`EquipmentDatabase.loadPulverizeFromText` + `PulverizeFlags.PULVERIZE_BITS`), AshP184 live `pulverize(item)` v1 (direct item-id aggregate at 1_000_000), AshP185 pulverize.txt upgrade/cluster rows (`deriveUpgrade`/`deriveCluster`), AshP186 live `pulverize(item)` v2 (`PulverizeAggregate` bitmask decode), AshP187 lazy `derivePulverization(id)` + `PulverizeImplications` + `isPulverizable`, AshP188 `initializePulverization()` login prefetch, AshP189 armory visit learn v2 (`StandardRewardDatabase.toData` session-log + `NpcStoreVisitOverlay` meat rows), AshP190 live `desc()` HTTP prefetch on cache miss, AshP191 generic shop inventory parse v1 (`ShopRowParser`/`ShopRowDatabase`/`ShopInventorySync` + shoprows.txt loader + visit learn session-log), AshP192 coinmaster validate v29 (visit-learned overlay probes), AshP193 shop inventory v2 (CONC concoction session-log + shops.txt `ShopType`/`craftingType` + MerchTable/Sammy `ShopRowParser` dedupe), AshP194 coinmaster validate v30 (visit-overlay authority via `hasVisitOverlay`), AshP195 shop inventory v3 (`FlowerTradeinSync` `ShopRowParser.parseSingleCostRows` dedupe + inverted Chroner/flower mapping + `Chroner (N)` stack counts), AshP196 coinmaster validate v31 (flowertradein visit-overlay authority + `hasTradeFlower` gate regression), AshP197 shop inventory v4 (TD2A `whichskill=` skill-row parse + `ItemStack.isSkill` + `ShopInventorySync` coinmaster learn + `ShopRowDatabase.parseItemOrMeatOrSkill`), AshP198 coinmaster validate v32 (visit-overlay skill probes + `containsBuySkill`/`visitInventorySkillAvailable` + `hasSkill` gate + empty-overlay authority fix), AshP199 shop inventory v5 (legacy buy/sell row split + `ShopRowDatabase.registerShop` + `parseShopNameFromHtml` + legacy buy/sell session-log format), AshP200 coinmaster validate v33 (visit-overlay sell probes + `containsSellItem`/`visitInventorySellAvailable` + inventory validate), AshP201 shop inventory v6 (`SkillDefinitionDatabase.registerFromShopVisit` + TD3A skill name parse + `ShopRowDatabase.promoteShopType` + visit-inferred shop types), AshP202 coinmaster validate v34 (dynamic skill shop visit overlay + optional desc_skill prefetch + `corpus_dynamicSkillShopVisit_live`), AshP203 shop inventory v7 (`CoinmasterData.visitShopRows`/`isDisabled` + `ShopRowDatabase.logVisits` + `ShopInventorySync` hook/disabled/newStyle + armory/flowertradein/crimbo25_sammy `applyVisitShopRows` dedupe), AshP204 coinmaster validate v35 (disabled visit-overlay authority + hook-driven dynamic shop validate + `corpus_disabledCoinmasterVisitOverlay_live`), AshP205 shop inventory v8 (`CoinmasterData.visitShop` + `MerchTableSync` conmerch hooks + disabled legacy classify), AshP206 coinmaster validate v36 (conmerch hook-driven visit overlay validate + `corpus_conmerchVisitOverlayValidate_live`), AshP207 shop inventory v9 (`visitShop` hook batch 2: trapper/driparmory/september/lathe/junkmagazine), AshP208 coinmaster validate v37 (hook-driven drip shield + trapper pref gates), AshP209 shop inventory v10 (`visitShop` hook batch 3: bacon/arcade/kiwi/mystic/shore/5dprinter), AshP210 coinmaster validate v38 (hook-driven mystic psychosis pixel + shore toaster gates), AshP211 shop inventory v11 (`visitShop` hook batch 4: mrreplica/blackmarket/piraterealm/chroner/crimbo23/swagger + final `CoinmasterShopSync` pref-sync dedupe), AshP212 coinmaster validate v39 (hook-driven mrreplica/blackmarket/piraterealm/swagger/alliedhq gates), AshP213 shoprows pref write-back + public `is_coinmaster_skill` ASH + remove no-op `CoinmasterShopSync.apply`, AshP214 coinmaster validate v40 (`is_coinmaster_skill` + learnedShopRows restore probes), AshP215 shop hook corpus v41 (AshP205?214 visit-hook ASH validate regression), AshP216 shop cleanup/validate v42 (delete dead `CoinmasterShopSync.apply` + learnedShopRows visit-overlay restore), AshP217 shop hook corpus v43 + AshP218 CoinmasterShopSync teardown v44 (`CoinmasterPurchasePrefs` extraction), AshP219 `PulverizeRequest` HTTP + `pulverize`/`smash` CLI, AshP220 `get_related(item,"pulverize")` ASH + login `initializePulverization` hook, AshP221 `get_related(item,"fold")` ASH + AshP222 `get_related(item,"zap")` ASH + AshP223 `get_zap_wand` ASH + AshP224 `ZapRequest` HTTP/`zap()` ASH/`zap` CLI + AshP225 `PulverizeRequest` tender-hammer retrieve preflight + AshP226 `UntinkerRequest` HTTP/`untinker` CLI + AshP227 `JunkListManager`/`common_junk.txt` junkList pref + AshP228 `CleanupJunkRunner`/`cleanup`/`junk` CLI + AshP229 FlaggedItems singleton/memento lists + AshP230 cleanup polish/Pulverize ronin junk auto-add/bare `untinker` quest CLI, AshP231 FlaggedItems profitableList + itemflags.txt import/export (`ItemFlagsParser` + `importItemFlags`/`exportItemFlags`), AshP232 `automall`/`quark` CLI (`AutoMallRunner` + `QuarkRunner`), AshP233 `UntinkerRequest.completeQuest` (Knoll + Degrassi Knoll Garage side-trip + `Quest.UNTINKER` sync + untinker auto-retry), AshP234 Loathing Legion universal screwdriver untinker (`inv_use.php?action=screw` item 4926 + `runUntinkerCli` routing), AshP235 `BastilleDatabase` (`bastille.txt` style-set loader) + AshP236 `BastilleBattalionSync` (choices 1313?1319 `_bastille*` pref sync) + AshP237 Bastille session-log parity (`registerRequest`/`logStrength`/`checkPredictions`) + AshP238 Bastille battle/cheese file logging (`BastilleBattalionFileLog` + `UserDataFileAppender`) + AshP239 `nonfilling.txt` loader (`ConsumableDatabase` NONFILLING + level-req lookups) + AshP240 `buffbots.txt` registry loader (`BuffBotDatabase` known-bot registry + opt-out gate) + AshP241 `$item[levelreq|fullness|quality|adventures|muscle|mysticality|moxie|inebriety|spleen|id|name|plural|descid|image]` consumable bracket fields via `ItemEntityFields` + AshP242 `$item[minhp|maxhp|minmp|maxmp]` restore bracket fields via `RestoreDatabase` expression eval + AshP243 `$item[quest|gift|tradeable|discardable|usable|multi|reusable|combat|combat_reusable|fancy|pasteable|smithable|cookable|mixable]` flag bracket fields via `ItemDatabase` attribute helpers + AshP244 `$item[notes|potion|chocolate|candy|candy_type|name_length]` metadata bracket fields via `ConsumableDatabase`/`ItemDatabase` helpers + AshP245 `$item[dailyusesleft]` via `ItemMaximumUses.maximumUses`/`DailyLimitDatabase`/`RestoreDatabase.restorationMaximum`, AshP246 `$item[smallimage|seller|buyer|skill|recipe|noob_skill]` via `ItemDatabase.getSmallImage`/`getNoobSkillId` + `CoinmasterRegistry.findSeller`/`findBuyer` + `ModifierDatabase` skill/recipe tags, AshP247 `$item[tcrs_name]` via `TCRSDatabase.getTCRSName` + pref-backed class/sign map load on TCRS login + `TCRSDatabase.applyModifiers`/`resetModifiers` login hooks via `ModifierDatabase.updateItem`/`resetOverrides` + `ConsumableDatabase.updateConsumable`/`resetOverrides`/`setLevelVariableConsumables`/`setVariableConsumables` + `ConcoctionDatabase.refreshConcoctions`/`refreshConcoctionsNow`/`markRecalculateAdventureRange` + `ConsumableDatabase.calculateAllAverageAdventures`/`getAverageAdventures` active-effect v2 (`AverageAdventureContext` + ode/lunch/gourmand/munchies/milk/rowdy lookup + sushi/stillsuit boost gate + slowcore zero) + `conditionalExtraAdventures` martini/wine/lasagna v1 + pizza/beans/saucy/bondcore martini v2 (`ConditionalExtraAdventureContext` + `HolidayCalendar.isMonday` + `AscensionPath.LICENSE_TO_ADVENTURE`) + `getAdventuresNeeded` v1 (`ConcoctionAdventureUsage`/`ConcoctionAdventuresNeeded`/`ConcoctionAdventuresContext` + cache craft-turn deduction in `calculateAverageAdventures`) + `refreshConcoctionsNow` initial counts v1 (`ConcoctionRuntimeState`/`ConcoctionRefreshContext` + login `refreshConcoctionsFromInventory`) + available ingredients v2 (`ConcoctionAvailableIngredients`/`ConcoctionIngredientSources` + login `refreshConcoctionsFromAggregated`) + calculate2 creatable v1 (`ConcoctionCreatable`/`canMake`/`calculateCreatableTotal` + `creatableCount`/`totalCount` in `refreshConcoctionsNow`) + `creatable_amount` runtime cache v1 + cachePermitted v1 (`ConcoctionRefreshContext.fromLiveSession` + `ConcoctionPermitted` in calculate2 pass + AshP122 `preferRuntime`) + calculate3/freeTotal v1 (`ConcoctionLimitPools` cachePermitted v2 + adventure pseudo-ingredient gating in `canMake` + `freeTotalCount`) + stills/clipArt/extrude limits v3 (`ConcoctionLimitPools` STILL/CLIPART/TERMINAL method caps + `inGLover` clip-art short-circuit in `canMake` + `ConcoctionRefreshContext.inGLover`) + meat limits v4 (`meatLimit` pool + `ConcoctionBuyables` price init + meat pseudo-ingredient + post-calculate2 creatable meat subtract + COMBINE meat-paste gate) + coinmaster refresh init v1 (`canUseCoinmasters`/`coinmasterAcquirable` + COINMASTER-only `skipCalculate` short-circuit + `canMake` COINMASTER branch) + queued limit pools v1 (`ConcoctionQueueBudget` + queued subtraction in `ConcoctionLimitPools`) + interchangeable ingredients v1 (`ConcoctionInterchangeableIngredients` + `get_ingredients`/`canMake`/`CreatableAmount` resolve-at-use-time) + craft queue budget v1 (`ConcoctionQueueReserve`/`ConcoctionCraftQueue` push/pop + `ConcoctionQueueBudget` increment on queue + `lastRefreshContext` refresh after push/pop) + storage pull queue budget v1 (`pullAmount`/`pullsUsed` reservation + `pullsBudgeted`/`applyPullablePass` + `storageCountById`/`considerPulls` refresh wiring) + consumption organ queue budget v1 (`ConcoctionOrganAmounts`/`ConcoctionOrganQueueReserve` + `queuedFullness`/`queuedInebriety`/`queuedSpleenHit`/`queuedMimeShotglass` + `ConsumptionEligibility.effective*Remaining`) + queued ingredient refresh v1 (`ConcoctionQueuedIngredients` per-bucket credits in `ConcoctionAvailableIngredients.aggregate` + `ConcoctionMayoQueue`/`lastQueuedMayo`/Mayodiol swap) + special queue counters v1 (`ConcoctionSpecialQueue` fancy-dog/speakeasy/s'more/affirmation/spaghetti/everfull/pirate-fork + `setSmoresData` on queue mutation) + pseudo-ingredient queue v1 (`ConcoctionQueuedPseudoIngredients` ADV/MEAT/PULL/TOME/STILL/EXTRUDE/FREE_CRAFT track/release + `totalsByType`/`reconcileWithBudget`) + visibleTotal v1 (`ConcoctionRuntimeState.visibleTotal`/`availableCount` + `ConcoctionLimitPool.visibleTotal` gross/net) + per-bucket craft queue v1 (`ConcoctionCraftQueue` FOOD/BOOZE/SPLEEN/POTION stacks + `pop(bucket)` + desktop-order `clear()` + bucket-local mayo/mime release) + creatable registry v1 (`ConcoctionCreatableRegistry`/`getCreatables()` + `wasPossible` on refresh tail) + fancy hot dog registry v1 (`HotDogDatabase`/`isFancyHotDog` + `ConcoctionQueueContext.isFancyDog`/`queuedFancyDog` queue wiring) + daily-limit queue gating v1 (`HotDogDatabase.canQueueFancyDog` + `SpeakeasyDatabase`/`canQueueSpeakeasyDrink` + craft-queue push guards) + daily-limit pref sync v1 (`ClanLoungeSync` `_fancyHotDogEaten`/`_speakeasyDrinksDrunk` lounge HTTP sync + `limitClan()` craft-queue push guards) + AshP292 speakeasy availability (`SpeakeasyAvailability`/`HotDogAvailability` parse + virtual SPEAKEASY/HOT_DOG concoction refresh tails + `ConcoctionDatabase.refreshAfterLoungeMutation` + `SPEAKEASY`/`HOT_DOG` method gates) + AshP295 ClanLoungeRequest hotdog/speakeasy HTTP + AshP296 `ConcoctionQueueRunner` inventory eat/drink queue drain (`RetrieveItemService` + `EatFoodRequest`/`DrinkBoozeRequest` + consume preflight + refresh tail fix) + AshP297 `ConcoctionCreateRequest` craft-only queue drain for auto-craftable itemId 0 concoctions + AshP298 `CafeRequest`/`CafePurchaseRequest` cafe queue drain v1 (Hell's Kitchen/Chez Snoot?e/Microbrewery `cafe.php` + `ConcoctionQueueRunner` consumeCafeItem) + AshP299 `StillSuitRequest` stillsuit distillate queue drain (`inventory.php?action=distill` + choice 1476 + `ConcoctionQueueRunner` consumeStillSuitItem) + AshP300 `ConcoctionConsumptionType.SPLEEN` + `ChewRequest` spleen queue drain (`inv_spleen.php` + retrieve + canChew/spleen preflight + `ConcoctionQueueRunner` consumeWithEatDrinkOrChew) + AshP293 virtual HOT_DOG concoctions + `applyHotDogRefreshTail` + hotdogstand/`eathotdog` lounge refresh + `HOT_DOG` method gate + `CreatableAmount` lounge totalCount) + `EffectDatabase.stripConsumableActions`/`addEffectSource`/`resetOverrides` + `ConcoctionDatabase.setEffectName`/`resetEffectNames` + `CafeDatabase` id?name index + TCRS cafe booze/food pref maps + `TCRSSkipItemIds` campground/chateau skips (Phase 258?261), AshP248 `$item[dailyusesleft]` v2 via `ItemMaximumUses` fight/choice/limit-mode/path guards + `buildItemUseLimitsContext`, AshP249 `$skill[id|name|type|level|image|traincost|class|libram|passive|buff|combat|spell|song|expression|walk|shanty|summon|permable|dailylimit|dailylimitpref|timescast]` via `SkillEntityFields`/`SkillDefinitionProxy`/`SkillCategory`, AshP251 `$effect[id|name|image|descid|quality|attributes|song|candy_tier|default|note|all]` via `EffectEntityFields`/`EffectDefinitionProxy`/`CandyEffectTier`, AshP253 `$familiar[id|name|image|attributes|hatchling|drop_name|drop_item|combat|physical_damage|elemental_damage|block|delevel|hp_during_combat|mp_during_combat|other_action_during_combat|hp_after_combat|mp_after_combat|other_action_after_combat|passive|underwater|variable]` via `FamiliarEntityFields`/`FamiliarDefinitionProxy` + AshP254 `$familiar[experience|owner|owner_id|charges|feasted|poke_level|drops_today|drops_limit|fights_today|fights_limit|poke_level_*|poke_move_*|poke_attribute|soup_weight|soup_attributes]` via `PokefamDatabase`/`fambattle.txt` + `FamiliarDailyStats` + `FamiliarManager`, AshP255 `$bounty[plural|type|kol_internal_type|number|image|monster|location]` via `BountyEntityFields`/`BountyDatabase`, AshP256 `$phylum[image]` via `PhylumEntityFields`/`PhylumNames.getImage()`, AshP257 `$element[image]` via `ElementEntityFields`/`ElementNames.getImage()`, AshP258 `$modifier[name|type]` via `ModifierEntityFields`/`ModifierNames`, AshP259 `$class[id|primestat|path]` via `ClassEntityFields`/`CharacterClass.ascensionPath`, AshP260 `$coinmaster[token|item|property|available_tokens|buys|sells|nickname|shopid]` via `CoinmasterEntityFields`/`CoinmasterData.availableTokens()` + property pref overrides, AshP261 interactive ASH soft defaults (`user_confirm`/`user_prompt` auto-yes/timeout defaults/first aggregate key; no native dialog UI), AshP262 interactive ASH `user_notify` headless no-ops (void; no system-tray UI), AshP263 buff request CLI + `request_buff` ASH (`buff bot skill [turns]` cliDispatch + `BuffBotManager.requestBuff` PM protocol + login `BuffBotDatabase.load()`), AshP264 BuffBot `getOffering` in `csend` (`BuffBotXmlParser` + philanthropic?standard meat conversion + login `configureOfferings` + bare `buff`?skills CLI alias), AshP265 faxbot/fax CLI + `faxbot`/`can_faxbot`/`send_fax` ASH (`FaxBotDatabase`/`FaxBotXmlParser` + `FaxBotManager` PM poll protocol + `ClanLoungeRequest` send/receive fax HTTP + login `FaxBotDatabase.configure` + `ChatManager` faxbot message capture), AshP266 `is_online` ASH + `ChatProbe` `/whois` probe + faxbot online gating (`FaxBotDatabase.canFaxbotOnline` + skip-offline-bot iteration), AshP267 chat command ASH (`slash_count` via `/count` probe + `chat_clan`/`chat_private`/`chat_macro` via `ChatProbe`/`ChatSender`), AshP268 chat who/player ASH (`who_clan`/`get_player_id`/`get_player_name` via `PlayerIdRegistry`/`ChatHtmlParser` + live chat poll id capture), AshP269 chat_notify ASH (`chat_notify` via `ChatManager.notify` ? `#events` channel + `ChatColor`/`ChatScreen` color rendering), modifier lookups via `modifiers/*`; roadmap: `2026-06-11-ash-script-full-parity.md`).
- Run unit tests with `.\gradlew.bat :shared:jvmTest`; verify Android builds with `.\gradlew.bat :androidApp:assembleDebug`; on Windows PowerShell, chain shell commands with `;`, not `&&`.
- Incremental parity phases use Quest/CLI/Maximizer tracks; Phases 53?217 advanced Tier 1 (AshP19?AshP115 entity/modifier/monster/combat libraries including MONSTER/LOCATION/PATH brackets, combat adjustment, drop/XP/initiative/meat/item drops, monster combat stats/element/`jump_chance`, location `jump_chance`, hit/miss APIs, `all_monsters_with_id`, expression Init/Atk/Def/HP, numeric + expression Scale/Cap/Floor, Exp/`base_mainstat_exp`, `REDUCE_ENEMY_DEFENSE`, Phys/Elem resistance brackets, `MLMult:` effective ML, ML phys-res boost, `$monster[raw_*]` / `$monster[base_*]` effective, `monster_initiative` initPenalty, `$monster[poison|group|manuel_name|wiki_name]`, `$monster[attack_elements]` multi-EA, `$monster[sub_types|images]`, `$monster[random_modifiers|attributes]` (runtime via MonsterStatusTracker + EncounterModifierPipeline), RandomModifierStats lazy apply on last_monster, monster beeosity (Beecore) on effective stats, Intergnat demonName12 sync from fight HTML, equipment beeosity (Maximizer/BHY), Summoning Chamber `summon` CLI, quest-log demonName sync, Yeg demonName13 cargo scrap sync, demonName14 segment tracking + `demons solve14` CLI, Allied Radio HTTP + `alliedradio` CLI, QUEST_LOG modifier-expression consequences (`royalty` pref via `ConsequenceActionResolver`), cargo-cult picked-pocket sync + `cargo` CLI + `picked_pockets`/`picked_scraps` ASH, PocketDatabase + cargo count/list/typed-pick + `pocket_monster`/`available_pocket`/`pick_pocket` ASH, cargo pocket introspection + `registerPocketFight`, DESC_ITEM consequence expressions + `ItemDescriptionConsequenceSync` + `roman()`/`monstername`, DESC_ITEM `=mods` enchantment parse + `ModifierDatabase.overrideModifier` + `DynamicItemModifierSync`, DESC_EFFECT consequence expressions + `=mods` + `EffectDescriptionConsequenceSync` + effect cached pref restore, DESC_SKILL consequence expressions + `SkillDescriptionConsequenceSync` + desc_skill self=true hook, COMBAT_SKILL consequence expressions + `CombatSkillConsequenceSync` + fight dropdown hook, MONSTER disambiguation consequences + `MonsterConsequenceSync` + `disambiguateMonster`, dynamic modifier checkMods v1 + `checkDynamicModifiers` on login/refresh, consequence `updateOneDesc` rotation prefetch, extended checkMods v2, login desc checks v3 (crown/bjorn/bird-of-day/entauntauned/savage beast + closet KGB/Baseball), skill-granting equipment conditional skills + coat-of-paint class-change refresh, Eternity codpiece `eternitycod` API + CODPIECE1?5 slots + gem modifiers + codpiece conditional skills + `have_skill` equipment merge, crown/bjorn familiar desc sync (`Current Occupant` race parse), Throne/Bjorn modifier accumulation in `CurrentModifiers`, `ItemAvailability`/`LimitModeGates` itemAvailable gates, post-bird `SkillLearner.learnSkill` + `fetchSkills()`, Eternity codpiece gem equip HTTP (`EquipmentRequest` docodpiece + choice 1588), `buildCheckContext` storage/stash IDs + Hermit clover coinmaster gate + `AccessibleItemCount` for `available_amount`/retrieve check-only, battle `learnSkillFromResponse` (NEWSKILL parse + item consumption + leveled pref increment), `AccessibleItemCount` depth (LoL/Sea storage gates + worthless id 13 + Hat Trick + inactive familiar equipment), `AccessibleItemCount` storage gates (Thrifty/Standard restriction + ronin freepull split), `StandardRequest.isAllowed` full parity (Trendy/Thrifty evergreen/Quantum familiar), `FamiliarUsability`/`have_familiar` path gates + retrieve restricted-item early exit, `in_terrarium` ownership-only ASH + Maximizer familiar switch usability + `RestrictionListRefresh` on retrieve, `canUseFamiliars`/Quantum gates + `use_familiar`/CLI/enthrone/bjornify usability + Maximizer enthrone/bjorn filtering, AshP109 CharacterState resource getters + `my_path_id` + `EntityIntrospection` name/extended to_int, AshP110 charpane/combat class-resource sync, AshP111 campground garden sync (`GardenSync` + live `my_garden_type`), AshP112 closet+storage meat sync + AshP113 session meat tracking (`SessionMeatSync` + live `my_session_meat`), AshP114 desc(entity) cache-only description lookups (`DescriptionCache` + live `desc()` for item/effect/skill), AshP115 storage pulls remaining sync (`pulls_remaining` + `StorageMeatSync` pullsleft parse), AshP116 stills + mushroom plot ASH (`stills_available` gate logic + `StillSync`; `have_mushroom_plot` + `MushroomPlotSync`), AshP117 `craft_type(item)` concoction method descriptions (`CraftTypeDescription`), AshP118 `refresh_status`/`restore_hp`/`restore_mp` (`RecoveryManager.checkpointedRecoverHp/Mp`), AshP119 `mood_execute` + zodiac sign booleans (`ZodiacSign` + `MoodManager.checkpointedExecute`), AshP120 consumption limit ASH (`ConsumptionEligibility` + `can_eat`/`can_drink` + capacity limits), AshP121 item property ASH (`ItemDatabase`/`EquipmentDatabase` + trade/gift/discard/display/plural/power/hands/type), AshP122 craft introspection ASH (`get_ingredients`/`creatable_amount` + `CreatableAmount`), AshP123 shop probes + concoction pricing (`is_npc_item`/`is_coinmaster_item`/`concoction_price`), AshP124 craft permission gating + `creatable_turns` + path-base modifier-effective consumption limits (`ConcoctionPermitted`/`CreatableTurns`/`ConcoctionCreationCost`), `$monster[fact|fact_type]`, `$monster[min_sprinkles|max_sprinkles]`, CLI long-tail including `journey`/`witchess`/`volcano`, charpane sync for Ed/VYKEA/pasta thrall/class resources, AshP125 recursive `creatable_turns` + AshP126 free crafting ASH, AshP127 shop validate probes + AshP128 ConcoctionPermitted depth + AshP129 concoction method gates + AshP130 NPC shop validate v2 + AshP131 concoction method gates v2 + AshP132 NPC shop validate v3 + AshP133 concoction method gates v3 + AshP134 NPC shop validate v4 + AshP135 kitchen craft gates + AshP136 NPC shop validate v5 + AshP137 STAFF/PHINEAS/kitchen fancy v2 + AshP138 NpcShopSync/coinmaster validate v6 + AshP139 kitchen auto-buy v3 (`KitchenAutoBuy`/`BoxServantAvailability` willBuyTool/willBuyServant + COOK/MIX_FANCY v3) + AshP140 coinmaster validate v7 (Jarlsberg/Swagger/Crimbo17 gates + `is_npc_item(id)` 1-arg) + AshP141 craft gates v4 (SMITH/SSMITH/CLIPART/JEWELRY + `KnollAvailability`/`SmithingGates`) + AshP142 coinmaster validate v8 (Crimbo20/shore/replica/pixel + `DesertBeachAccessibility`/`ReplicaMrStoreAccessibility`) + AshP143 ConcoctionPermitted v3 (TIKI/TorsoAwareness + explicit ROLL/SEWER/MUSE/SUSE) + AshP145 ConcoctionPermitted v4 (`ConcoctionMethodAliases` compound tokens + gnomads beach/KoE + CLIPART `skillsRecalled`) + AshP146 coinmaster validate v10 (bacon/arcade/dv/kiwi/fixodent + `FolderHolderAccessibility`) + AshP147 shop visit sync v1 + AshP148 validate v11 (bugbear/wildfire NPC + mystic psychosis pixels) + AshP149 shop visit sync v2 (hippy/fwshop/mayoclinic/shore/dv visit+purchase pref sync + `NpcBuyRequest` wildfire buy wiring) + AshP150 validate v12 (sync-driven hippy peach/fwshop hat/shore toaster/dv flask probes) + AshP151 shop visit sync v3 (`SwaggerShopSync` + jarl cosmic six-pack purchase hook + `NpcShopSync` hiddentavern unlock + `store.php`/`peevpee.php` visit hooks) + AshP152 validate v13 (sync-driven hiddentavern/swagger/jarl probes) + AshP153 shop visit sync v4 (`CoinmasterShopSync` mrreplica year + blackmarket MACGUFFIN unlock) + AshP154 validate v14 (sync-driven mrreplica/blackmarket probes) + AshP155 shop visit sync v5 (`FunALogUnlockPrefs` + piraterealm unlock prefs/`availableFunPoints`) + AshP156 validate v15 (sync-driven PirateRealm Fun-a-Log probes) + AshP157 shop visit sync v6 (`DripArmoryPrefs` + driparmory `drippyShieldUnlocked`) + AshP158 validate v16 (sync-driven Drip Armory shield probes) + AshP159 shop visit sync v7 (`TimeTowerSync`/`TimeTowerAccessibility` + Chroner shops + `place=twitch` + Swagger visit fix) + AshP160 validate v17 (toolbelt free-pull pref gate + Allied HQ flak shield probes) + AshP161 shop visit sync v8 (`TrapperSync`/`SpinMasterLatheSync`/`SeptEmberSync`/`JunkMagazineSync`) + AshP162 validate v18 + AshP165 free-pull collections + AshP166 validate v20 (`get_free_pulls`/`get_cached_free_pulls` + `storage_amount` freepull sum + `TinkeringBenchPurchasedItem` + `FlowerTradeinAccessibility`) + AshP167 shop visit sync v10 (`FlowerTradeinSync` + MerchTable token prefs) + AshP168 validate v21 (flowertradein dynamic validate + `get_storage()` dual-cache refresh) + AshP169 shop visit sync v11 (`Crimbo25SammySync`) + AshP170 validate v22 (conmerch tattoo + Sammy wad-trade validate) + AshP181 armory refresh + AshP182 validate v28, AshP183 pulverize.txt loader + AshP184 `pulverize(item)` v1 + AshP185 upgrade/cluster loader + AshP186 `pulverize(item)` v2, AshP187 lazy pulverize derive + AshP188 login prefetch + AshP189 armory visit learn v2 + AshP190 desc() HTTP prefetch + AshP191 shop inventory parse + AshP192 validate v29, AshP193 shop inventory v2 + AshP194 validate v30 + AshP195 shop inventory v3 + AshP196 validate v31 + AshP197 shop inventory v4 + AshP198 validate v32 + AshP199 shop inventory v5 + AshP200 validate v33 + AshP201 shop inventory v6 + AshP202 validate v34 + AshP203 shop inventory v7 + AshP204 validate v35 + AshP205 shop inventory v8 + AshP206 validate v36 + AshP207 shop inventory v9 + AshP208 validate v37, AshP209 shop inventory v10 + AshP210 validate v38 + AshP211 shop inventory v11 (`ReplicaMrStoreSync`/`BlackMarketShopSync`/`PirateRealmShopSync`/`ChronerShopSync`/`Crimbo23ShopSync.applyVisitShop`/`SwaggerShopSync`) + AshP212 validate v39, AshP213 shoprows pref write-back + `is_coinmaster_skill` ASH + AshP214 validate v40, AshP215 shop hook corpus v41 + AshP216 cleanup/validate v42, AshP217 shop hook corpus v43 + AshP218 CoinmasterShopSync teardown v44 + AshP219 pulverize HTTP/CLI + AshP220 get_related pulverize ASH + AshP221 get_related fold ASH + AshP222 get_related zap ASH + AshP223 get_zap_wand + AshP224 Zap HTTP/ASH/CLI + AshP225 pulverize retrieve polish + AshP226 untinker HTTP/CLI + AshP227 junkList persistence + AshP228 cleanup junk CLI + AshP229 singleton/memento flagged lists + AshP230 cleanup polish/Pulverize ronin junk auto-add/bare `untinker` quest CLI + AshP231 profitableList/itemflags + AshP232 automall/quark CLI + AshP233 untinker quest side-trip + AshP234 LL screwdriver untinker + AshP235 BastilleDatabase + AshP236 BastilleBattalionSync choice pref sync + AshP237 Bastille session-log parity + AshP238 Bastille battle/cheese file logging + AshP239 nonfilling.txt loader + AshP240 buffbots.txt registry loader + AshP241 `$item[...]` consumable bracket fields via `ItemEntityFields` + AshP242 `$item[minhp|maxhp|minmp|maxmp]` restore bracket fields, AshP243 `$item[quest|gift|tradeable|...]` flag bracket fields + AshP244 `$item[notes|potion|chocolate|candy|candy_type|name_length]` metadata bracket fields + AshP245 `$item[dailyusesleft]` via `ItemMaximumUses` + AshP246 `$item[smallimage|seller|buyer|skill|recipe|noob_skill]` + AshP248 `$item[dailyusesleft]` v2 via `ItemMaximumUses` + AshP249?AshP250 `$skill[...]` via `SkillEntityFields`/`SkillDefinitionProxy` + AshP251?AshP252 `$effect[...]` via `EffectEntityFields`/`EffectDefinitionProxy` + AshP253?AshP254 `$familiar[field]` via `FamiliarEntityFields`/`FamiliarDefinitionProxy`/`PokefamDatabase`/`FamiliarDailyStats` + AshP256?AshP257 `$phylum[field]`/`$element[field]` image brackets + AshP258?AshP259 `$modifier[field]`/`$class[field]` via `ModifierEntityFields`/`ClassEntityFields` + AshP260 `$coinmaster[field]` via `CoinmasterEntityFields`/`CoinmasterData.availableTokens()` + AshP261 interactive ASH soft defaults (`user_confirm`/`user_prompt`) + AshP262 interactive ASH `user_notify` headless no-ops + AshP263 buff request CLI/`request_buff` ASH + AshP264 BuffBot offering fetch + `csend` philanthropic meat conversion + AshP265 faxbot/fax CLI + `faxbot`/`can_faxbot`/`send_fax` ASH (`faxbot/` package + `faxbots.txt` registry/XML load) + AshP266 `is_online` ASH + `ChatProbe` whois online gating for faxbot + AshP267 chat command ASH (`slash_count`/`chat_clan`/`chat_private`/`chat_macro`) + AshP269 chat_notify ASH (`chat_notify` via `ChatManager.notify` ? `#events` channel + `ChatColor`/`ChatScreen` color rendering), tower/hedge maze/PirateRealm sync); `GameRuntimeLibrary.REVISION` uses `phaseNN` format (currently `phase4490`); Phases 258?317 TCRS applyModifiers track includes AshP295 `ConcoctionQueueRunner` lounge queue drain + `StandardRequest` CLAN_ITEMS gates + `ClanHotdogMenuCache` + Phase 296 inventory eat/drink queue drain + Phase 297 craft-only queue drain (`ConcoctionCreateRequest`) + Phase 298 cafe purchase queue drain (`CafePurchaseRequest`/`HellKitchenRequest`/`ChezSnooteeRequest`/`MicroBreweryRequest`) + Phase 299 stillsuit distillate queue drain (`StillSuitRequest`) + Phase 300 spleen queue drain (`ChewRequest`/`ConcoctionConsumptionType.SPLEEN`) + Phase 301 potion queue drain (`UseItemRequest`/`ConcoctionConsumptionType.USE`) + `CrimboCafeRequest` + `ClanManager`/`ClanIdSync` + AshP301 `get_clan_id`/`get_clan_name` + Phase 302 ghost/hobo binge drain (`UseItemRequest.binge`/`ConcoctionConsumptionType.GLUTTONOUS_GHOST`/`SPIRIT_HOBO`) + FLOUNDRY lounge buy drain (`FloundryDatabase`/`FloundryRequest`) + `addCreationQueue` failure re-queue (`ConcoctionCraftQueue.restore`) + Phase 303 slimeling/robortender/mimic familiar feed (`UseItemRequest.feedCandy`/`robooze` + ASH/CLI `ghost`/`hobo`/`slimeling`/`robo`) + Phase 304 queue drain CLI wiring (`eatqueue`/`drinkqueue`/`chewqueue`/`usequeue`/`ghostqueue`/`hoboqueue`/`slimelingqueue`/`roboequeue`/`createqueue`) + `ConcoctionConsumptionType.NONE` create-only drain + `create` ASH/CLI ? `ConcoctionCreateRequest` + Phase 305 live Floundry HTML parse (`FloundryAvailability`/`AshP305 get_fishing_locations`) + Phase 306 consumption-helper re-queue + partial `lastUnconsumed` re-queue (`ConsumptionHelperState`/`EatFoodRequest`/`DrinkBoozeRequest` helper routing) + Phase 307 CreateItemRequest router v1 (`StillCreateRequest`/`ConcoctionCreateRequest` STILL shop buy + COINMASTER `buyItem` + `isCreateSupported`/`stillShopRow` + queue drain/`create` ASH wiring) + Phase 308 CreateItemRequest router v2 (`ConcoctionData.param` CLIPART parser + `ClipArtCreateRequest`/`RollingPinCreateRequest` + expanded `isCreateSupported` + queue drain/`create` ASH wiring) + Phase 309 TERMINAL extrude drain (`TerminalRequest`/`TerminalExtrudeCreateRequest`/`FalloutShelterRequest` + expanded `isCreateSupported` + queue drain/`create` ASH wiring) + Phase 310 CreateItemRequest router v4 (`SewerCreateRequest` gum retrieve/closet shuffle/`inv_use` loop + worthless-item alias + expanded `isCreateSupported` + queue drain/`create` ASH wiring) + Phase 311 CreateItemRequest router v5 (`VykeaCreateRequest`/`VykeaChoiceMapper` instructions + choice 1120?1123 chain + expanded `isCreateSupported` + queue drain/`create` ASH wiring) + Phase 312 CreateItemRequest router v6 (`MuseCreateRequest`/`UseItemRequest.multiUse` first-ingredient multi-use + expanded `isCreateSupported` + queue drain/`create` ASH wiring) + Phase 313 CreateItemRequest router v7 (`PhineasCreateRequest` volcanoisland.php sealhide POST + expanded `isCreateSupported` + queue drain/`create` ASH wiring) + Phase 314 CreateItemRequest router v8 (`StaffCreateRequest` guild.php makestaff POST + expanded `isCreateSupported` + queue drain/`create` ASH wiring) + Phase 315 CreateItemRequest router v9 (`GnomeTinkerCreateRequest` gnomes.php tinksomething POST + expanded `isCreateSupported` + queue drain/`create` ASH wiring) + Phase 316 CreateItemRequest router v10 (`SushiChoiceMapper`/`SushiCreateRequest` sushi.php create-and-consume POST + `isCreateAndConsume` queue runner fix + expanded `isCreateSupported` + queue drain/`create` ASH wiring) + Phase 317 CreateItemRequest v2 (`CreateItemIngredients`/`makeIngredients` two-pass retrieve + `RetrieveItemService` specialty sub-craft + TINKER/STAFF/PHINEAS/SUSHI handler wiring) + Phase 318 SUSHI v2 (`SushiConsumptionSync`/`SushiChoiceMapper.resultNameFromFormFields` post-create ingredient accounting + fullness/doily/worktea sync + session-log registration in `SushiCreateRequest`) + Phase 319 CreateItemRequest v3 (`ConcoctionMeatPasteNeeded` meat-paste COMBINE/JEWELRY preflight + `CreateItemIngredients.makeIngredients` state/initialCount + Muse/Vykea/Terminal/Sewer specialty handler wiring) + Phase 320 CreateItemRequest v4 (`ConcoctionCreateRequest` station/SUSE `makeIngredients` + `RetrieveItemService.craftSuse`/`craftAtStation` + `StillCreateRequest` + lazy `createItemIngredientsProvider` DI) + Phase 321 sushi.php visit sync (`SushiChoiceMapper.formFieldsFromUrl` + `SushiConsumptionSync.parseConsumptionFromVisit` + `GameRuntimeLibrary.processVisitResponseHooks` + `eventBus` DI) + Phase 322 RollingPin makeDough (`RollingPinCreateRequest` NPC wad buy fallback + tool/hand batch + >10-without-pin guard) + Phase 323 DreadScrollManager v1 (`DreadScrollManager` 8 clue parsers + fight/choice/inv_use/skills/sushi hooks + `dreadscroll` CLI + `recordFailure` choice 703) + Phase 324 MalusCreateRequest v11 (`MalusCreateRequest` guild.php `malussmash` POST + `isMalusCraftable`/`isCreateSupported` + `ConcoctionCreateRequest` v11 router + `SkillManager` Pulverize gate + queue drain/`create` ASH wiring) + Phase 325 JewelCreateRequest v12 (`JewelCreateRequest` craft.php combine POST + `isJewelCraftable`/`isCreateSupported` + `ConcoctionCreateRequest` v12 router + `ConcoctionMeatPasteNeeded` JEWEL/EJEWEL paste parity + queue drain/`create` ASH wiring) + Phase 326 BarrelCreateRequest v13 (`BarrelCreateRequest` da.php barrel shrine + choice 1100 + `BarrelChoiceMapper` pref gates + `isBarrelCraftable`/`isCreateSupported` + `ConcoctionCreateRequest` v13 router + queue drain/`create` ASH wiring) + Phase 327 BarrelShrineSync v68 (`BarrelShrineSync` choice 1100 visit/post-choice pref sync + `barrelShrineUnlocked` unlock + visit/choice hooks + create-success concoction refresh) + Phase 328 GuildVisitSync v69 (`GuildVisitSync`/`GuildCreationSync` guild.php visit store-open pref sync + `malussmash`/`makestaff` post-response ingredient accounting + `ConcoctionDatabase.chefStaffByBaseItemId`/`malusByIngredientItemId` + create-request success tails + `processVisitResponseHooks` wiring) + Phase 329 barrelprayer CLI v70 (`BarrelPrayerRequest`/`BarrelChoiceMapper.findPrayer` + `barrelprayer` CLI protection/glamour/vigor/buff via `da.php?barrelshrine=1` + choice 1100 + desktop preflight gates) + Phase 330 GuildSkillSync v71 (`GuildSkillSync` guild.php `buyskill` meat deduct + `SkillLearner.learnSkill` + concoction refresh + `SkillDefinitionProxy.findSkillFromUrl`/`getGuildPurchaseCost` + classskills `Level:` parse + visit hooks) + Phase 331 DreadScroll success v72 (`DreadScrollManager.handleHighPriestSuccess` choice 703 + `isMerkinHighPriest`/`merkinQuestPath=scholar` pref sync + dreadscroll consumption + visit/use hooks) + Phase 332 DreadScroll gladiator v73 (`DreadScrollManager.handleGladiatorChampionSuccess` sigil-burn use path + `isMerkinGladiatorChampion`/`merkinQuestPath=gladiator`/`lastColosseumRoundWon=15` pref sync + `parseDreadscrollUse` router + inv_use visit hooks) + Phase 333 Merkin quest done v74 (`MerkinQuestSync` choice 709/713/717 boss-victory pref sync + `shubJigguwattDefeated`/`yogUrtDefeated`/`merkinQuestPath=done` + visit/applyFromResponse hooks) + Phases 334?353 TCRS mood/uneffect/ManaBurn track (v75?v94: `SeaMerkinSync`, `GuildQuestSync`/`DesertBeachUnlockSync`, `UneffectRemovableMaps`/`UneffectActionResolver`, mood uneffect routing/autofill/CLI/`MoodRemovalTriggerExecution`, `MoodSettingsFile`/`UserDataFileIO` `{username}_moods.txt` persistence, `AtSongEviction`/`AtSongSlotTracker`, `ManaBurnManager`/`ManaBurn`/`BreakfastBurnSkills`/`LibramSkillCasts` + `lastChanceBurn` CLI + `simulateBalancedCasts`) + Phases 361?397 Maximizer Evaluator/CheckedItem track (`Evaluator` v1?v5 + `MaximizerSlot`/`SlotList`/`MaximizerEquipmentEnumerator` ranked buckets + `MaximizerCheckedItem`/`MaximizerCheckedItemBuilder` acquisition channels + `MaximizerCheckedItem.validate`/`pullBuyable`/`canUseMallToStorage` v1 + global loadout gates `totalMin`/`totalMax`/`exceeded`/failed-aware speculation + `MaximizerAutoContext` automatic outfit/synergy/hobo/brimstone/cloathing bucket pins + `EquipmentDatabase.nextEquipmentItemId` full equipment DB scan + `MaximizerPriceLevel` mall historical gate + `MaximizerSynergyAdjustments` synergy post-compare + `MaximizerOutfitAdjustments` outfit post-compare + `MaximizerOutfitSpeculation` tryOutfits speculation + `MaximizerFamiliarSpeculation` tryAll per-familiar loop + `MaximizerFamiliarItemSpeculation` tryFamiliarItems + `SlotList.getFamiliar(i)` per-familiar FAMILIAR bucket ranking + `MaximizerContainerSpeculation` tryContainers + `MaximizerFoldDedup` fold-group speculation dedup + CONTAINER in searchSlots + `MaximizerHatSpeculation` Crown/Bjorn per-familiar branching + `MaximizerCarriedFamiliars` carriedFamiliars auto-discovery + `Modeable`/`ModeableState`/`MaximizerModeSelection` modeable best-mode pre-selection + mode-aware `CurrentModifiers`/`scoreLoadout` + Phase 380 `MaximizeSpec.forcedModeables`/`+shield`/`sea` goal-forced modes + `ModeableRequest`/`ModeableChoiceSync` mode CLI HTTP + `maximize()`/`speculate()` mode emission + `MaximizeResult.modeSwitched` + `mustEquipAfterChange` re-equip + Phase 381 modeable backup slots/`assignmentModeOverrides` + `EquipmentSlot.CARDSLEEVE`/`MaximizerCardSelection` best-card pre-pass + `MaximizerFoldDedup` in main `speculate()` DFS + `MaximizerScoringOptions` threading + Phase 382 `MaximizerGarbageAuto` shirt/champagne required/automatic pins + `Evaluator` `+current`/`-current`/`+outfit`/`-outfit`/`equip`/`checkEquipment` + zero-delta equipped-item keep + `MaximizerCheckedItem.passesEmitMallCheck` `PriceLevel.ALL` emit mall re-check + `MaximizerRankedItem.required` bucket trim + Phase 383 `MaximizerLetterBonus` + `Evaluator` `bonus`/`letter`/`number`/`plumber`/`cold plumber` parse + `equipmentBonus`/`itemBonus` in `scoreLoadout`/`scoreItem` + `negEquip` enumeration skip + `resolvePlumberTools` in `buildMaximizePlan` + `MaximizeGoal` `-equip` constraint + Phase 384 `MaximizerEmitSlot`/`MaximizerBoost`/`RetrieveItemSimulator`/`MaximizerBoostExecutor` emitSlot boost/retrieve chains + adventure-cost suffix + `maximize()`/`speculate()` boost output + `MaximizeResult.boosts`); Phase 385 folder/sticker/bootskin sub-slots (`EquipmentSlot` FOLDER/STICKER/BOOTSKIN/BOOTSPUR + api.php stickers/folder_holder sync + `CurrentModifiers` parent-gated sub-slot mods + `MaximizerSubSlotPreservation` + KoLHS folder-holder gate + `ALL_EMIT_SLOTS` emit + card-sleeve emit); Phase 386 `MaximizerBoostCostSuffix`/`MaximizerNonEquipmentBoosts` cost suffixes + horsery/effect post-equipment boosts + `CurrentModifiers` horsery + activeEffects overlay scoring); Phase 387 boombox/MCD non-equipment boosts + `MaximizerBoostVerboseSuffix` verbose brackets + `MaximizerMcdAvailability` + `HorseryRequest`/`BoomBoxRequest`/`MindControlRequest` + `horsery`/`boombox`/`mcd` CLI + `CurrentModifiers` boomBox/MCD overlay + `ExpressionContext` mindControlLevel); Phase 388 noobcore absorb boosts v1 (`ItemDatabase.getItemListByNoobSkillId` + `NoobcoreAbsorbs` + `CurrentModifiers` customModifierOverlay + `MaximizerNoobcoreAbsorbBoosts` skill/equipment absorb paths + `getAbsorbable` retrieve chains + `AbsorbRequest` + `absorb` CLI); Phase 389 `MaximizerBoostSourceRules` exotic effect-source rules v1 (cast/cargo/alliedradio/barrelprayer/photobooth/clan hot-dog/speakeasy + `SkillCastCosts`/`SkillRequiredItemForEffect`); Phase 390 `MaximizerBoostSourceRules` exotic effect-source rules v2 (friars/pillkeeper/pool/shower/swim/jukebox/ballpit + VIP-key/limitClan/canInteract/spleen pref gates); Phase 391 `MaximizerBoostSourceRules` exotic effect-source rules v3 (telescope/fortune/mom/concert/summon/mayosoak + quest-finished/workshed/demon-name pref gates); Phase 392 `MaximizerBoostSourceRules` exotic effect-source rules v4 (witchess/monorail/toggle/crossstreams/monkeypaw/genie + `CampgroundItemSync.hasWitchessSet`); Phase 393 `MaximizerBoostSourceRules` exotic effect-source rules v5 (gong/styx/play/skeleton + `itemIdFromSource` gong/deck/skeleton prefix mapping); Phase 394 `MaximizerBoostSourceRules` exotic effect-source rules v6 (gap/spacegate/daycare/vault3/grim/aprilband + `familiarManager` boost context DI + `LimitModeGates.limitFamiliars`); Phase 395 `MaximizerBoostSourceRules` exotic effect-source rules v7 (terminal enhance/campaway cloud/loathingidol/mayam/asdonmartin drive + `MayamAvailability`/`CampAwayAvailability` + `CampgroundItemSync.asdonMartinFuel` + loathingidol/mayam `itemIdFromSource` mapping) + Phase 396 `MaximizerBoostSourceRules` exotic effect-source rules v8 (beach head/skate/hatter/synthesize + `BeachHeadAvailability`/`SkateParkAvailability`/`RabbitHoleAvailability`/`CandyDatabase` + `CandyEffectTier.getEffectModulus`) + Phase 397 `MaximizerBoostSourceRules` consumption effect-source rules v1 (`ConsumptionSourceRule` + `MaximizerConsumptionGates` for use/eat/drink/chew + G-Lover/cafe/TCRS/vintner wine/trivia master/box of sunshine gates + `ItemDatabase` consumption item IDs) + Phase 398 `MaximizerFilterType`/`MaximizerFilters` filterType v1 (`maximizerLastFilters` pref + EQUIP enumeration skip/EQUIP-only path + OTHER/CAST/FOOD/BOOZE/SPLEEN/USABLE/WISH per-source gating + WISH genie/monkeypaw injection + ASH maximize 3-arg/5-arg filter overloads) + Phase 399 Maximizer includeAll + bang potion resolution v1 (`maximizerIncludeAll` pref wiring + `BangPotionResolver`/`lastBangPotion*`/`lastSlimeVial*` lookup in `ConsumptionSourceRule`/`itemIdFromTarget` + `ItemDatabase` bang/slime ID constants) + Phase 400 Maximizer non-equipment boost execution v1 (`MaximizerManager.cliExecutor` + `MaximizerBoostExecutor` CLI fallback + non-equipment `maximize()` pass + cast-only success fix) + Phase 401 Maximizer mutex violations v1 (`ModifierDatabase.rebuildMutexBits` + `ModifierValues` MUTEX_VIOLATIONS merge + `MaximizerMutexViolations` speculation/effect-gain gating + `MaximizerBoost.priority` sort) + Phase 402 Maximizer replaceable effect mutex v1 (`ModifierDatabase.rebuildReplaceableMutexEffects` + `getReplaceableMutexFor` + `ReplaceableEffectMutex.applyEffectGain` + `EffectDefinitionProxy` bracket-id parse) + Phase 403 Maximizer `_spec` write-back v1 (`ModifierValuesFormatter` + `ModifierDatabase.overrideGenerated` + `MaximizerSpecWriteBack` maximize/speculate hook + ASH Type:name modifier lookup) + Phase 404 Maximizer continuation + search status v1 (`MaximizerContinuation` forceContinue/abort/permitsContinue + CLI abort/stop wiring + `ComboBudget`/`MaximizerSearchStatus` limit/score-cap/interrupted output) + Phase 405 Maximizer live post-equip rescore v1 (`NonEquipmentBaseline` + `scorePostEquipmentLive` + `maximize()` equipment-then-live-non-equipment rebuild) + Phase 406 Maximizer post-equip status refresh v1 + Phase 407 Maximizer showProgress v1 (`MaximizerProgress` throttled progress + `progressDisplay`/`#events` notify + speculate progress line) + Phase 408 Maximizer charpane status fallback v1 (`CharacterStatusRefresh`/`CharpaneStatusSync`/`CharpaneEffectsSync` + `KoLCharacter.updateFromCharpane` + `LimitModeGates.requiresCharPane` + `MaximizerPostEquipRefresh`/`refresh_status`/`refreshCharacterStates` routing) + Phase 409 Maximizer pre-search status refresh v1 (`MaximizerPreSearchRefresh` + `buildMaximizePlan` entry refresh before `scoreBefore`/enumeration via `CharacterStatusRefresh` api/charpane routing) + Phase 410 Maximizer activeEffects scoring overlay v1 (`MaximizerScoringOptions.activeEffects` + post-refresh overlay in `scoreBefore`/speculate DFS/`scoreAfter`/mode+card pre-passes + `MaximizerPostEquipRefresh` + live `scoreAfter` + `writeFromLiveState`) + Phase 411 Maximizer scoreItem activeEffects overlay v1 (`searchActiveEffects` + marginal `scoreLoadout` delta in `scoreItem` when effects present + enumerate/greedy seeding wiring) + Phase 412 Valhalla charpane status routing v1 (`CharpaneValhallaSync`/`KoLCharacter.applyValhallaState` + `CharacterStatusRefresh` Valhalla-first routing + `EffectManager.clearEffects` + `bankedKarma` pref + logout reset + recovery guard) + Phase 413 Quantum terrarium pre-refresh v1 (`QuantumTerrariumSync`/`QuantumTerrariumRequest` + `CharacterStatusRefresh.refreshWithQuantumPreflight` + `MaximizerPreSearchRefresh`/`refresh_status`/`refreshCharacterStates`/`InventoryManager` wiring + `nextQuantumFamiliar*`/`_nextQuantumAlignment` pref sync) + Phase 414 Maximizer passive skill overlay v1 (`MaximizerPassiveSkills`/`MaximizerScoringOptions.passiveSkillNames` + `MaximizerManager.searchPassiveSkillNames` + speculate/sub-pass propagation + `scoreItem` full-loadout delta when passives present) + Phase 415 Maximizer passive skill overlay v2 (`MaximizerPassiveSkills.resolve`/`isPassiveSkill` Vampyre parity + `SkillGrantingEquipmentSync` equipment merge + `MaximizerPreSearchRefresh.fetchSkills` + `ChoiceContext`/`MaximizerManager.buildCheckContext`); Phase 416 Quantum terrarium v2 (`TurnCounter` quantum familiar/Q.F.I.D.M.A. counters + `QuantumTerrariumRequest.checkCounter`/`forceAlign` + visit hooks + needsStatusRefresh follow-up) + Phase 417 Pokefam charpane team sync v1 (`CharpanePokefamSync`/`PokefamTeamSlot` + `FamiliarDefinitionDatabase.getByImage` + `CharacterState.pokeTeam` + charpane refresh when `inPokefam`) + Phase 418 Pokefam ASH + familiar merge v2 (`my_poke_fam`/`KoLCharacter.pokeFamSlot` + live `$familiar[poke_level]` + `FamiliarManager.mergePokeTeam`/`FamiliarData.pokeLevel` + charpane familiarManager wiring) + Phase 419 Pokefam fight HTML sync v1 (`FightPokefamSync` round-1 your-team parse + `PokefamTeamSlot` power/hp/attributes + fight.php visit/adventure-loop hooks) + Phase 420 Pokefam soup ASH sync v1 (`FamiliarSoupSync` terrarium soup JSON + `FamiliarData.soupWeight`/`soupAttributes` + protogenetic soup use + live `$familiar[soup_weight]`/`soup_attributes`) + Phase 421 Pokefam PokeBoost v1 (`PokeBoost`/`PokefamBoostSync` famteam.php feed pref sync + `FightPokefamSync` boost-adjusted power/hp/attributes) + Phase 422 Pokefam fight HTML sync v2 (`FightPokefamSync` 6-table parse + enemy session-log lines + `PokefamMoveRegistry` move discovery + `PokefamDatabase.registerFromFight` runtime overlay + `SessionLogger` hook wiring) + Phase 423 Pokefam FamTeam sync v1 (`FamTeamSync` famteam.php active/bullpen parse + shared `FightPokefamSync.parseFamteamTable` + `FamiliarManager.registerPokefamFamiliar` + `my_familiar_weight` soup overlay) + Phase 424 Pokefam FamTeam HTTP + session-log v2 (`FamTeamSync.registerRequest` + `PokefamBoostSync` pokepill inventory consumption + `FamTeamRequest` visit/feed HTTP + login famteam prefetch) + Phase 425 Quest tracking depth v2 (`QuestLogProgress.findQuestProgress` + `QuestSpecialSync` section parsers + `QuestLogRequest.parsePage` null-on-unparseable + `QuestFightRules` Telegram/Party Fair combat hooks) + Phase 426 KoLCharacter collection cache v1 (`CollectionCacheSync` login seed + mutation refresh for `closet_amount`/`storage_amount`/`stash_amount`) + Phase 427 Quest tracking depth v3 (`QuestDatabase.setQuestIfBetter` + BAT quest-log guard + `QuestLogDatabase.detectStep` fuzzy passes + Party Fair woots combat quest-log resync) + Phase 428 KoLCharacter collection cache v2 (display login prefetch + `CACHED_DISPLAY` seed via `SessionManager.buildIngredientSources`) + Phase 429 KoLCharacter campground ASH v1 (`get_workshed`/`have_campground`/`have_chef`/`have_bartender` + `CampgroundAvailability` + bookshelf sync) + Phase 430 KoLCharacter ascension ASH v1 (`limit_mode`/`in_casual`/`turns_played`/`my_turncount`/`total_turns_played`/`daycount` + `LimitModeNames` + `casual`/`daynumber` API sync) + Phase 431 KoLCharacter per-quest counter writers v1 (`ShadowRiftSync`/`_shadowRiftCombats` + `DispensarySync`/`lastDispensaryOpen` visit hooks) + Phase 432 KoLCharacter campground ASH v2 (`get_dwelling`/`get_campground` + `DwellingSync`/`CampgroundInventorySync` visit hooks) + Phase 433 KoLCharacter garden yield + mushroom plot squares v1 (`GardenCropSync`/`GardenCropAvailability` + `MushroomPlotSync` 4?4 grid + `BreakfastManager` harvest gate) + Phase 434 Quest tracking depth v4 (`GuzzlrCombatSync`/`guzzlrDeliveryProgress` fight increment + delivery completion + `AdventureManager` post-fight hook) + Phase 435 Quest tracking depth v5 (`FinalQuestCombatSync` NS contest crowd decrement + FINAL tower monster step writers + `AdventureManager` post-fight hook) + Phase 436 Quest tracking depth v6 (`ToppingPeakCombatSync`/`oilPeakProgress`/`booPeakProgress` fight decrement + dress-pants/lovebug bonuses + `AdventureManager` post-fight hook) + Phase 437 Quest tracking depth v7 (`IslandWarCombatSync`/`IslandWarBattlefieldMessages` battlefield message-table delta + `hippiesDefeated`/`fratboysDefeated` increment + `AdventureManager` post-fight hook) + Phase 438 Quest tracking depth v8 (`IslandWarCombatSync.applyEndOfWar`/`finishWar` boss/explosion end-of-war + `sideDefeated`/`warProgress`/`ISLAND_WAR`/`HIPPY_FRAT` finish + counter skip + `AdventureManager` post-fight hook) + Phase 439 Quest tracking depth v9 (`IslandWarCombatSync.applyNunsSidequestWin` brigand NC ? `sidequestNunsCompleted` + `QuestFightRules.applyGhostBossReset` GHOST boss reset + `AdventureManager` post-fight hook) + Phase 440 Quest tracking depth v10 (`IslandWarVisitSync`/`parseBattlefield` `warProgress=started` + bfleft/bfright map clamp + `processVisitResponseHooks` bigisland.php wiring) + Phase 441 Quest tracking depth v11 (`IslandWarVisitSync.parseQuest` + `parseArena`/`parseFarm`/`parseNunnery` sidequest visit sync + flyer consume + `nunsVisits`) + Phase 442 Quest tracking depth v12 (`IslandWarVisitSync.parseOrchard`/`parseLighthouse` + `OutfitPool.WAR_HIPPY_OUTFIT` + queen-heart/gunpowder consume + hippy-store prefs + concoction refresh) + Phase 443 Quest tracking depth v13 (`IslandWarVisitSync.parseJunkyard` tool/location pref tracking + molybdenum tool consume + `sidequestJunkyardCompleted` + JUNKYARD routing) + Phase 444 Quest tracking depth v14 (`IslandWarVisitSync.parseCamp`/`parseCampTokenBalance` Dimemaster/Quartersmaster `availableDimes`/`availableQuarters` + whichcamp routing) + Phase 445 Quest tracking depth v15 (`IslandWarVisitSync.applyFromPostwarIslandVisit`/`deduceWinner` + post-war arena/nunnery + `postwarisland.php` hook) + Phase 446 Quest tracking depth v16 (`IslandWarBattlefieldMessages` victory/area/hero/finish formatters + `IslandWarCombatSync` session-log on combat win/end-of-war + `AdventureManager` post-fight hook) + Phase 447 Quest tracking depth v17 (`IslandWarCampSync` getgear/turnin pref updates + session-log trading lines + camp coinmaster metadata + bigisland hook) + Phase 448 Quest tracking depth v18 (`IslandWarBattlefieldMonsters` combats.txt classification + unknown/unexpected battlefield session-log + monster-gated `applyCombatWin` + LocationDatabase 132/140/296 snarfblat fix) + Phase 449 Quest tracking depth v19 (`IslandWarVisitLogSync` bigisland visit session-log + `_lastCampVisited` + bossfight `LAST_LOCATION`/HQ routing + `GameRuntimeLibrary` bigisland hook) + Phase 450 Quest tracking depth v20 (`CoinmasterManager` camp buy/sell response hook + `IslandWarCampSync`/`IslandWarVisitLogSync` + dimemaster/quartersmaster URL fix + `ConcoctionDatabase.markRefreshNeeded`) + Phase 451 Quest tracking depth v21 (`IslandWarVisitLogSync` postwarisland.php action/place session-log + `GameRuntimeLibrary` postwar hook) + Phase 452 Quest tracking depth v22 (`IslandWarActionResponseSync` concertVisited/_farmerItemsCollected/pyro gunpowder + visit hook wiring) + Phase 453 Quest tracking depth v23 (`IslandWarResetSync` lastBattlefieldReset/resetIsland + `SessionManager` login hook) + Phase 660 FireExtinguisherCombatSync skills 7386-7390 + Phase 661 QuestCombatWinExtrasSync bosses/Rufus/chasm + Phase 662 BugbearManager.handleKeyotron + Phase 663 NewYouCombatSync + Phase 664 AirportRadioChoiceSync choice 984 + Phase 665 AirportNpcChoiceSync 915-917 + Phase 666 Zeppelin cabin/Spectre/Wu Tang + Phase 667 PirateRealm named bosses + SeaCombatSync + Phase 668 QuestItemUsedSync bowling/lair/bomb/DNA + Phase 669 DailyDungeonChoiceSync 689-693 + Phase 670 TwinPeakChoiceSync + Phase 671 Clancy 571-577 + Island War 142/146 + Manor 921 + Phase 672 TelegramChoiceSync 1171-1175 + Phase 673 PartyFairChoiceSync 1322-1328 + Phase 674 LightsOutChoiceSync 890-903 + Phase 675 DinseyKioskChoiceSync 1066+1073 + Phase 676 ToppingPeakNcSync choice 611 + Phase 677 Palindome 517 + Pyramid 132/929 + Sea 299/302-308 + Phase 678 WoodsDemonChoiceSync 560-569 + Phase 679 WalfordChoiceSync 1114-1116 + Phase 680 MeatsmithChoiceSync 1059+1060 + Phase 681 Garbage 679 + Phase 682 DinseyKioskChoiceSync 1067 + Phase 683 FightItemPrefSync flyers+DNA + Phase 684 MayoMinderChoiceSync 1076 + Phase 685 Garbage 669-671 + Phase 686 Palindome 872 + Phase 687 Lights Out visit+dollie + Phase 688 Party Fair trash questlog + Phase 689 FightItemPrefSync mayo/beehive/knife/blank-out + Phase 690 MadnessBakeryChoiceSync 1080 + Phase 691 MadnessBakeryChoiceSync 1081 + Phase 692 AutopsyChoiceSync 589 + Phase 693 ManorTowelChoiceSync 882 + Phase 694 DmtChoiceSync 1119 + Phase 695 FightItemPrefSync EMPTY_EYE + Phase 696 MadnessBakeryChoiceSync 1084 + Phase 697 GarbageBeanstalkSync 675 + Phase 698 LostKeyChoiceSync 594 + Phase 699 SnojoChoiceSync 1118 + Phase 700 BatfellowChoiceSync 1133/1134 + Phase 701 BatfellowChoiceSync 1168 + Phase 702 BugbearChoiceSync 588 + Phase 703 FireStartingKitChoiceSync 595 + Phase 704 SkeletonClosetChoiceSync 603 + Phase 705 NumberologyChoiceSync 1103 + Phase 706 TeaTreeChoiceSync 1104 + Phase 707 TeaTreeChoiceSync 1105 + Phase 708 GnomePartChoiceSync 597 + Phase 709 ZombieBaitChoiceSync 599 + Phase 710 SpoopyChoiceSync 1110 post + Phase 711 SpoopyChoiceSync 1110 visit + Phase 712 BatfellowItemChoiceSync 1140-1142 + Phase 713 BatfellowItemChoiceSync 1143-1148 + Phase 714 OracleChoiceSync 1190 + Phase 715 TimeSpinnerChoiceSync 1195 + Phase 716 TimeSpinnerChoiceSync 1196 + Phase 717 GingerbreadClockChoiceSync 1215 + Phase 718 LoveTunnelChoiceSync 1222 + Phase 719 SeaJellyChoiceSync 1219 + Phase 480 CLI long-tail v25 (`rest` campground alias + `second`/`hold`/`dualwield` + `colorecho`/`cecho` + `events`/`EventHistory` + `prefref` + `poolskill`) + Phases 481?486 PvP (`PvpManager`/`PeeVPeeRequest`/`PeeVPeeSync`/`ProfileRequest` + AshP481 `pvp_attacks_left`/`hippy_stone_broken`/`current_pvp_stances` + `pvp`/`flowers`/`attack`/`swagger`/`steal` CLI + fight-result parse) + Phase 487 CLI long-tail skills list v26 (`cliSkills` + `pass`/`passive`/`self`/`combat` aliases + bare `cast`/`skill` list) + Phase 488 CLI long-tail effects/inv listing v27 (`cliEffects`/`cliInventory` + AT song-count + leftover filter) + Phase 489 CLI long-tail closet/familiars listing v28 (`cliCloset`/`cliFamiliars` + `closet`/`closet list [filter]` + `familiars [filter]`/`familiar`/`familiar list [filter]`) + Phase 490 CLI long-tail storage/display listing v29 (`cliStorage`/`cliDisplay` + `storage [filter]` + `display [filter]`); update `docs/parity-audit.md` after each phase.
- Quest state sync combines QuestAdvanceRules, QuestChoiceRules, QuestFightRules, QuestItemRules, QuestLogSync, QuestSpecialSync, and dedicated `quest/*Sync.kt` visit parsers (e.g. TowerSync, TelescopeSync, PirateRealmSync, IslandWarVisitSync/IslandWarActionResponseSync/IslandWarResetSync/IslandWarConcert, FireExtinguisherCombatSync, NewYouCombatSync, AirportRadioChoiceSync, AirportNpcChoiceSync, SeaCombatSync, DailyDungeonChoiceSync, TwinPeakChoiceSync, TelegramChoiceSync, PartyFairChoiceSync, LightsOutChoiceSync, DinseyKioskChoiceSync, WoodsDemonChoiceSync, WalfordChoiceSync, MeatsmithChoiceSync, MayoMinderChoiceSync, MadnessBakeryChoiceSync, AutopsyChoiceSync, ManorTowelChoiceSync, DmtChoiceSync, LostKeyChoiceSync, SnojoChoiceSync, BatfellowChoiceSync, BugbearChoiceSync, FireStartingKitChoiceSync, SkeletonClosetChoiceSync, NumberologyChoiceSync, TeaTreeChoiceSync, GnomePartChoiceSync, ZombieBaitChoiceSync, SpoopyChoiceSync, BatfellowItemChoiceSync, OracleChoiceSync, TimeSpinnerChoiceSync, GingerbreadClockChoiceSync, LoveTunnelChoiceSync, SeaJellyChoiceSync, VillainLairChoiceSync, MonorailChoiceSync, VykeaChoiceSync, SpacegateVaccinatorChoiceSync, SleazeAirportExtendedChoiceSync, BatfellowUpgradeChoiceSync, DeckChoiceSync, BlechHouseChoiceSync, TrickOrTreatChoiceSync, ArchSpadeChoiceSync, AutomatedFutureChoiceSync, CrimboShrubChoiceSync, MobiusChoiceSync, BaseballChoiceSync, DripHallChoiceSync, MushyCenterChoiceSync, HorseryChoiceSync, MimicDnaChoiceSync, LegendaryDigestionChoiceSync, AwolChoiceSync, StalagmiteChoiceSync, DrippyTreeChoiceSync, PowerPlantChoiceSync, ColdMedicineChoiceSync, BwApronChoiceSync, PhotoBoothChoiceSync, PlumberShopChoiceSync, BackupCameraChoiceSync, WildfireNpcChoiceSync, WildfireCaptainChoiceSync, CrystalBallChoiceSync, JuneCleaverChoiceSync, AutumnatonChoiceSync, TrainsetChoiceSync, BurningLeavesChoiceSync, PantogramChoiceSync, MummeryChoiceSync, YouRobotChoiceSync, ElfGratitudeChoiceSync, WoolChoiceSync, SitCourseChoiceSync, AprilBandChoiceSync, MayamChoiceSync, TakerSpaceChoiceSync, AntiScientificChoiceSync, BodyguardChoiceSync, CandyDevilerChoiceSync, SpecimenBenchChoiceSync, LeprecondoChoiceSync, PerilChoiceSync, PeridotChoiceSync, SeadentWaveChoiceSync, CouncilChoiceSync, CrimboPastChoiceSync, MonkeyPawChoiceSync, DigGiftChoiceSync, CoolerYetiChoiceSync, LockPickedChoiceSync, EntauntaunedChoiceSync, MappingMonstersChoiceSync, RetroCapeChoiceSync, CartographyChoiceSync, CatBurglarChoiceSync, FavoriteBirdChoiceSync, InfernoDiscoChoiceSync, SausageGrinderChoiceSync, BoomBoxChoiceSync, GarbageToteChoiceSync, PillKeeperChoiceSync, RedSnapperChoiceSync, DoctorBagChoiceSync, VoteBallotChoiceSync, MotorbikeChoiceSync, GrimChoiceSync, ClanFortuneChoiceSync, DaycareLobbyChoiceSync, GenieChoiceSync, ControlPanelChoiceSync, DartPerksChoiceSync, HashingChoiceSync, HybridizationChoiceSync, GnasirChoiceSync, IceHouseChoiceSync, DaycareChoiceSync, LanguageFluencyChoiceSync, FantasyRealmChoiceSync, LatteChoiceSync, CanadianWildlifeChoiceSync, FightItemPrefSync); Cyrpt in `session/CryptManager.kt`; Bugbear mothership key-o-tron in `session/BugbearManager.kt`; Ed servants in `servant/EdServantManager.kt` with `EdServantChoiceSync`/`EdServantCharpaneSync`; VYKEA companion in `vykea/VykeaCompanionManager.kt` with `VykeaCharpaneSync`; pasta thrall in `thrall/PastaThrallManager.kt` with `PastaThrallCharpaneSync`; class/path resources in `character/ClassResourceCharpaneSync.kt`/`ClassResourceCombatSync.kt`; campground garden in `campground/GardenSync.kt`; still shop + mushroom plot in `concoction/StillSync.kt`/`campground/MushroomPlotSync.kt`; closet/storage/session meat in `inventory/ClosetMeatSync.kt`/`StorageMeatSync.kt`/`SessionMeatSync.kt`; entity description cache + live HTTP prefetch in `data/DescriptionCache.kt`/`EntityIntrospection.kt`; armory visit learn in `shop/ArmoryAndLeggerySync.kt`/`NpcStoreVisitOverlay.kt`; generic shop inventory parse in `shop/ShopRowParser.kt`/`ShopRowDatabase.kt`/`ShopInventorySync.kt`; bundled data loaders: `journeyman.txt` via `data/JourneymanDatabase.kt`, `bastille.txt` via `data/BastilleDatabase.kt`/`session/BastilleBattalionSync.kt`, `buffbots.txt` via `buffbot/BuffBotDatabase.kt`, `faxbots.txt` via `faxbot/FaxBotDatabase.kt`, `witchess_solutions.txt` via `data/WitchessSolutionDatabase.kt`/`session/WitchessManager.kt`, `volcanomaze.txt` via `data/VolcanoMazeDatabase.kt`/`session/VolcanoMazeManager.kt`, `ocean.txt` via `data/OceanDatabase.kt`/`session/OceanManager.kt`/`request/OceanRequest.kt` (Phase 247?248 destination map + HTTP sailing automation on Poop Deck redirect), `wereprofessor.txt` via `data/WereProfessorDatabase.kt`/`request/ResearchBenchRequest.kt`/`session/WereProfessorResearchSync.kt`/`session/WereProfessorManager.kt` (Phase 249?251 research skill tree + choice 1523 HTTP/pref sync + `wereprofessor research` CLI), `defaults.txt` via `data/DefaultsDatabase.kt` (Phase 252?256 pref default loader + `Preferences` bundled-default fallback + login `seedMissingDefaults` write-back + `roa`/`rof` reset hooks on ascension/combat + `resetDailies`/`resetPerRolloverPrefs` + `RolloverCounterReset` cat-burglar/kolhs/TurnCounter on rollover + Phase 262 `BanishManager.resetRollover` + mayonnaise window carryover + Phase 263 `TrackManager.resetRollover` for trackedMonsters/trackedPhyla), `TCRS.astral_pets.txt`/`TCRS.astral_consumables.txt` via `data/TCRSAstralDatabase.kt` (Phase 253 astral shop summary lookup), `cultshorts.txt` via `data/PocketDatabase.kt`/`data/CultShortsDatabase.kt`/`session/YegDemonNameSync.kt`; tower automation in `TowerDoorRunner.kt`/`TowerDoorConfig.kt`; hedge maze in `HedgeMazeRunner.kt`/`HedgeMazeConfig.kt`; PvP in `session/PvpManager.kt`/`PeeVPeeSync.kt`/`request/PeeVPeeRequest.kt`/`ProfileRequest.kt` with AshP481 query ASH + `GameRuntimeLibrary.PvpCli.kt` (`steal` PvP alias, `steal N item` familiar-steal fallback); PvP registerRequest/printLine/profile fame-clan/swagger parseBalance-buy/ITEM_PATTERN live *(Phases 720?725)*; Quest ChoiceControl v183?v188 VillainLair/Monorail/VYKEA/Spacegate/Sleaze/Bat upgrades *(Phases 726?731)*; Quest ChoiceControl v189?v194 Deck/Villain1260+1262/Rufus/Blech/ToT/ArchSpade *(Phases 732?737)*; Quest ChoiceControl v195?v200 DeckCardsSeen/AutomatedFuture/CrimboShrub/Mobius/Baseball/DripHall *(Phases 738?743)*; Quest ChoiceControl v201?v206 Mushy/Horsery/Mimic/Digestion/AWOL/Stalagmite *(Phases 744?749)*; Quest ChoiceControl v207?v212 Drippy/PowerPlant/ColdMed/BwApron/PhotoBooth *(Phases 750?755)*; Quest ChoiceControl v213?v218 Plumber/BackupCam/Wildfire/CrystalBall/JuneCleaver *(Phases 756?761)*; Quest ChoiceControl v219?v278  *(Phases 767-828)*; live `pvp_attack`/`ranked_fam` ASH (AshP762/AshP763) + steal dual-route/famsteal (AshP764) + PvP track Resolved (phase778); skills list CLI via `GameRuntimeLibrary.SkillsCli.kt`; effects/inv/closet/familiars/storage/display listing CLI via `GameRuntimeLibrary.ShowDataCli.kt`; event dump CLI via `session/EventHistory.kt`.
- ASH runtime functions register via `regFn()` in `GameRuntimeLibrary.*.kt` under `ash/`; AshScope uses first-match overload resolution ? remove AshP11 stubs and wire live lookups in AshP12?P18 `resolveModifier*` helpers when adding AshP19+ behavioral batches; `AshCompatibilityCorpusTest` is the behavioral ASH regression corpus; JavaScript runtime and relay server are explicit parity non-goals.
- Git default branch is `master`; avoid stale `.worktrees/` for active development; exclude `.cursor/` hook state from commits unless requested.
- RabbitHole / Wumpus / Leaflet niche engines mega Phases 2811?2870 add chess Board/Square solving and choice 442/443 sync; Tea Party/Hatter request + CLI and choice 441 sync; Wumpus room deductions/Wumpinator with choice 360 and darkness fight hook; Leaflet request/state-machine automation and response routing.
- QuestManager residual hub mega Phases 2871?2930 adds the central URL router over existing quest sync parsers, residual visit-writer consolidation, combat/fight/item orchestration wrappers, and QuestLog page routing.
- Mall/Store economy residual mega Phases 2931?2990 adds paginated and single-store mall search, purchase response/store filters, fifth-cheapest cached and local historical pricing, StoreManager inventory/log parsing, batch automall/autosell, and ASH/CLI/RequestLogger/UI glue; runtime revision is `phase2990`.
- SorceressLair / NS tower residual mega Phases 2991?3050 adds the SorceressLairSync hub for prism liberation, Confidence-only chamber entry, contest/hedge visits, tower session logs, NS combat-item blocks, Nagamar preflight, behavioral hedge_maze ASH, and telescope challenge summaries.
- Spelunky / Bastille hybrid residual mega Phases 3051?3110 adds canonical Spelunky status migration, deep charpane/API equipment-resource parsing, GIF unlock and turn-40 inference, headless noncombat/logging/ASH helpers, plus Bastille reset, battle simulation, 12 cheese estimates, automated advisor and CLI; runtime revision is `phase3110`.
- Uneffect / Monster Manuel hybrid residual mega Phases 3111?3170 adds routed charsheet/Asdon/remedy uneffect HTTP, active/intrinsic/removable guards, response/inventory/concoction sync, CLI/mood/AT-song/Maximizer routing, effect-skill/item hooks and ASH maps, plus Monster Manuel questlog HTTP/cache, fight factoid parsing, metadata overlays and ASH cache access; runtime revision is `phase3170`.
- YouRobot / Grimstone / Rumple hybrid residual mega Phases 3171?3230 adds YouRobotManager part/CPU catalog with install energy/scrap accounting, robocore equip/familiar/potion gates, Robot modifier accumulation, ScrapheapRequest chronolith/scavenge deepen, robot CLI/ASH helpers, GrimstoneManager mask psychoses hub (stepmother/wolf/witch/hare), and RumpleManager materials/mastery/spy-sin-bribe with headless advisor and rumple CLI; runtime revision is `phase3230`.
- CakeArena / FamiliarTraining / BHH hybrid residual mega Phases 3231?3290 adds CakeArenaManager + CakeArenaRequest (opponent list, fight action=go, XP/suckage/session-log, adventuresUsed), FamiliarTool + headless FamiliarTrainingManager (Goals BASE/BUFFED/TURNS, FamiliarStatus weights/buffs, levelFamiliar/trainFamiliar/fightMatch/buffFamiliar, learnFamiliarParameters lite), live `train` CLI orchestration, and BountyHunterHunterRequest residual deepen (lucre, take/turnin, location/next-adventure prefs); runtime revision is `phase3290`.
- Grey You / Valhalla / Bad Moon / Spaaace hybrid residual mega Phases 3291?3350 adds GreyYouManager full GOO catalog + zoneAbsorptions/sortedGooSkills/modifier overlay + zone-grouped greyyou/gooskills CLI; ValhallaManager pre/on/post ascension + AfterLifeRequest karma/permery/deli/armory/reincarnate parse + CharpaneValhallaSync deepen; BadMoonManager 48-entry SPECIAL_ENCOUNTERS + EncounterManager pref writes + headless badmoon report; SpaaaceRequest transponder gate + Porko board/payout parse/load/solve + lastPorko* prefs + GENERATOR visit glue; runtime revision is `phase3350`.
- Hacienda / Leprecondo / Mushroom?Garden / Journey hybrid residual mega Phases 3351?3410 adds HaciendaManager haciendaLayout solver + choices 410?418/440 sync + clue/reward catalog + fight/key markers + recording prefs + HaciendaChoiceSync; LeprecondoManager furniture/need catalog + visit HTTP + discovery prefs + GoalManager GOAL_LEPRECONDO + familiar combat messages; MushroomManager plant/pick/harvest + 4?4 breeding forecast + MushroomRequest knoll HTTP + field CLI deepen; JourneyManager turn-threshold tracking + path gates + adventure-loop hooks + journeyman ASH/CLI; runtime revision is `phase3410`.
- Nemesis / Tavern / Dvorak / Mail / Spading residual mega Phases 3411–3470 adds headless Nemesis cave and paper-strip state, Tavern goofball/key parsing, Dvorak board solver, mailbox/contact caches, non-UI spading event boundaries, request/visit/session-log hooks, focused regression coverage, and runtime revision `phase3470`.
- FightRequest/combat lifecycle mega Phases 3471–3530 adds typed headless fight actions and multi-fight lifecycle state, action-cost/item-result parity, ordered structural fight HTML parsing, factbook/Eldritch/Crimbo/Chakra/pearl/proselytization handlers, familiar/IoTM hooks, FightRequest integration, focused regression coverage, and runtime revision `phase3530`.
- IoTM utility parity mega Phases 3531–3590 adds Cursed Magnifying Glass, Daylight Shavings Helmet, Dad Sea Monkee, Unusual Construct, Voting Booth, ChibiBuddy, Gourd Captain, and headless Ping request/manager state with ASH/CLI, fight/choice/visit/lifecycle wiring, focused regression coverage, and runtime revision `phase3590`.
- ASH surface/state-plumbing residual mega Phases 3591–3650 adds live restriction/trendy/thrifty ASH lookups, combat action buffers and character bonus helpers, bounded globally ranked Maximizer candidates with skill-aware effective-weapon/Fourth Saber gates, Action Bar request/cache/CLI support, acquired-item quantity parsing, and focused regression coverage; runtime revision `phase3650`.
- ASH semantic closure/live-state pipeline mega Phases 3651–3710 adds typed and generic modifier lookups, conservative response/result routing, timestamped filterable event history with preserved HTML, familiar default-equipment/favorite state, live Maximizer scoring, mall/shop semantics, and focused regression coverage; runtime revision `phase3710`.
- IoTM manager residual mega Phases 3711–3770 adds Locket catalog/fought/phylum parse, StillSuit fight-sweat + distillate mods, CrystalBall fight predictions, JuneCleaver fight colors, Trainset piece API, Florist plant catalog + `get_florist_plants`, Chateau furniture + `get_chateau`, `pingpong` CLI, and headless `git_*`/`svn_*` stubs; runtime revision `phase3770`.
- ChoiceAdventures catalog mega Phases 3771–3830 adds the desktop static spoiler/decision/item-goal tables (454 configurable + 66 spoilers), `parseChoicesWithSpoilers` catalog append, `pickGoalChoice` automation override, `choice-goal` CLI, and ChoiceCost fish-scale/bar-skin expand; runtime revision `phase3830`.
- GuildUnlock + Beach residual mega Phases 3831–3890 adds typed `GuildRequest`/`GuildUnlockManager` orchestration, inventory-delta `AdventureManager` stop results, Moxie pants restoration, canonical `BeachManager`, Beach Comb request lifecycle/accounting/logging, Desert Beach fallbacks, Skate Park reset/equipment/error handling, and `complete quest guild`; runtime revision `phase3890`.
- CLI Tier-4 mega Phases 3891–3950 adds Dad and Slimeling local reporters, clan status/snapshot/stash-log/refresh, validated local TCRS operators, explicit live spade kmail submission with partial-failure preservation, typed Pandamonium/Sven solve flow, and alias/help closure; runtime revision `phase3950`; GUI/Relay, JavaScript, full TCRS dumps, and desktop scripting remain non-goals.
- Residual HTTP request mega Phases 3951–4010 adds typed Hashing Vise, Potted Tea Tree, KGB, Foresee, Pizza Cube, Flea Market buy/sell, and read-only Ascension History requests with Umbrella mode validation and Palm Frond/MUSE accounting, plus CLI/help/DI closure (`vise`/`teatree`/`umbrella`/`foresee`/`kgb`/`flea`/`fleamarket`/`ascensionhistory`; no `pizza` CLI); runtime revision `phase4010`; GUI/Relay, JavaScript, full TCRS dumps, and desktop scripting remain non-goals.
- Ed servant + GoalManager + Mall + Ascension deepen mega Phases 4011–4070 adds EdServantHtmlFormatter catalog table + servants/servant CLI, GoalConditionParser count-based conditions + condition add/remove/set/list CLI, MallSearchRequest item-detail descitem parse, AscensionHistory player header cache, and AfterLife reincarnate logging; runtime revision `phase4070`.
- Non-goals reopen mega Phases 4131–4190 adds TCRSRemoteFetch GitHub pull + `tcrs fetch` CLI + TCRSDeriver/DescriptionParser, live minimal SimpleXPath/HtmlTreeParser for common KoL xpath patterns, and headless MallSearchDecorator mall-search buy-button/forbidden-store HTML; RelayServer/JavaScript/full TCRS derive sweep remain non-goals; runtime revision `phase4190`.
- Low-priority residual closure mega Phases 4191–4250 adds GoalConditionParser pseudo/substat/health/outfit conditions + SubstatCalculator, ChatterboxingChoiceSync choice 191 + FightBanishSync, ItemDatabase.getMatchingNames + MallSearchOverlay finalizeList + MallSearchRelayHook, AscensionSnapshotCache + xpath child-path corpus + is_dark_mode pref; runtime revision `phase4250`.
- Desktop r29219 combined-modifiers mega Phases 4311–4370 adds `DoubleModifier` combined tags (`Maximum HP / MP`, `All Attributes`, `All Attributes Percent`) + subsumed eval, desktop `modifiers.txt`/`statuseffects.txt` sync, `numeric_modifier` item `ModifierDatabase` fallback, `last_maximizer_succeeded` ASH, `$monster[blue_vs_red_team]` + desktop `monsters.txt`, Eternity Codpiece-first noncombat skill equip, TCRS notes/bitmap bit reuse; runtime revision `phase4370`; RelayServer/JS/full TCRS derive remain non-goals.
- Behavioral deepen mega V Phases 4441–4450 adds `current_maximizer_score()` no-arg via `maximizerList`/`lastMaximizeGoal`, `goal_count("item"|"items")` inventory-aware remaining sums, and HTTP request residual typed hubs (`SeaMerkinRequest`, `VolcanoIslandRequest` slime harvest/robe discard + `TrophyHutRequest` buytrophy meat/session-log); runtime revision `phase4450`; RelayServer/JS/full TCRS derive remain non-goals.
- Behavioral deepen mega VI Phases 4451–4460 adds live `is_goal(item)` via `GoalManager` + `resolveAshItemId`/`to_item(id)` id parity, `goal_exists` desktop alias parity via `GoalManager.matchesConditionType` + outfit/item split flag, `goal_count` floundry/leprecondo aliases + text factoid, `gametime_to_int`/`format_date_time`/date helpers via platform `expect`/`actual`, `receive_fax` via `FaxBotManager.receiveFaxOnly`, `numeric_modifier` STAT/SERVANT/PHYLUM via `ModifierDatabase` + familiar 4-arg overload, `banished_by` corpus; runtime revision `phase4460`; RelayServer/JS/full TCRS derive remain non-goals.
- Behavioral deepen mega VII Phases 4461–4470 adds `can_walk_from_choice`, `reverse_numberology`, `monster_factoids_available`, `current_rad_sickness`, `dart_skills_to_parts`, `every_card_name`, `clear_food_helper`/`clear_booze_helper`, `pre_validate_adventure`, live `pickpocket`, `leetify`/`stat_bonus_today`/`stat_bonus_tomorrow`, no-arg `absorbed_monsters` map, `last_item_message`; runtime revision `phase4470`; RelayServer/JS/full TCRS derive remain non-goals.
- Quest state-machine deepen mega Phases 4471–4480 adds `VisitChoiceQuestHooks` dual-path visit glue, Doctor Bag questlog refetch on empty item + ResultProcessor DOCTOR_BAG-only, PirateRealm visit unlocks 1347–1349 + cursed-compass sell remove, Party Fair DJ `processMeat`+session-log, Telegram office visit option capture, `QuestAdvanceRules` past-STARTED sail/party/telegram/doctor signals; runtime revision `phase4480`; RelayServer/JS/full TCRS derive remain non-goals.
- Behavioral deepen mega VIII Phases 4481–4490 adds `last_skill_message`, `eight_bit_points`, `get_no_pulls`, `get_items_hash`, `beret_busking_effects`, `image_to_monster`, fixed `dart_parts_to_skills`/`extract_items`/`sells_skill`/`sell_cost`, `path_name_to_id`/`path_id_to_name`; runtime revision `phase4490`; RelayServer/JS/full TCRS derive remain non-goals.
- Behavioral deepen mega IV Phases 4431–4440 adds banish fight-HTML alternate pattern closure (`AdventureParser`/`FightBanishSync` + `BANISHING_SHOUT` refuse guard), `is_banished(phylum)` ASH, `goal_count("meat"|"level")`, and `AshCompatibilityCorpusTest` banish-phylum/goal-count corpus; runtime revision `phase4440`; RelayServer/JS/full TCRS derive remain non-goals.
- Behavioral deepen mega III Phases 4371–4430 adds FightBanish residual (`ORDER_A_KNEECAPPING` + fight HTML patterns), `OutfitBitmapBonuses` Brimstone/Cloathing/McHugeLarge half-set scaling + per-item bitmap masks + `ModifierValues.bitmapCount`, Breakfast `checkJackass`/`collectSeaJelly` desktop flows + `AshCompatibilityCorpusTest` banish/maximizer/outfit-bitmap snippets; runtime revision `phase4430`; RelayServer/JS/full TCRS derive remain non-goals.
- Behavioral residual deepen mega II Phases 4251–4310 adds `goal_count` ASH + goal_exists autostop split, FightBanishSync fallback expansion, AscensionSnapshotCache extended summary + `ascensionhistory summary`, mall own-store relay highlight + fuzzy search retry; runtime revision `phase4310`.
- Low-priority deepen-partials mega Phases 4071–4130 adds Manuel factoid count goal decrement on fight win (`monstermanuel.gif` + `MonsterManuelManager.reset`), MallSearchOverlay NPC/coinmaster rows in mall search (`MallListingSource`), AscensionHistoryCompare snapshot delta on consecutive fetches + `ascensionhistory` CLI compare lines; runtime revision `phase4130`.
- 2026-09-01 parity audit recount at `phase4430`: 1,536 commonMain files / 158,210 LOC / 8,483 tests / 1,034 ASH `regFn` / 97 managers / 165 requests / 147 ChoiceSync; desktop 1,179 Java / 330,945 LOC / 321 requests / 95 managers (~48% LOC). Top Priorities reprioritized: ASH behavioral depth, banish fight-HTML (~19), HTTP Request residual, quest/ascension/clan deepen-partials; stale deferrals closed (Ed servant, GoalManager, mall overlay, ascension snapshot, is_dark_mode). RelayServer/JS/full TCRS derive remain non-goals.
- 2026-09-02 recount after Phases 4481–4490: 1,543 commonMain files / 8,548 tests / 1,061 ASH `regFn`; Behavioral Deepen VIII (`last_skill_message`, eight_bit, no_pulls, items_hash, beret busking, image_to_monster, dart/extract/sell fixes, path id/name); runtime revision `phase4490`; RelayServer/JS/full TCRS derive remain non-goals.
- 2026-09-02 recount after Phases 4471–4480: 1,542 commonMain files / 8,536 tests / 1,050 ASH `regFn`; Quest state-machine deepen (VisitChoiceQuestHooks, Doctor Bag refetch, PirateRealm visit unlocks+compass, Party Fair DJ meat, Telegram visit, AdvanceRules past STARTED); runtime revision `phase4480`; RelayServer/JS/full TCRS derive remain non-goals.
- 2026-09-02 recount after Phases 4461–4470: 1,541 commonMain files / 8,526 tests / 1,050 ASH `regFn`; Behavioral Deepen VII (`can_walk_from_choice`, numberology reverse, manuel factoids, rad sickness, dart skills, every-card, helpers, pre_validate, pickpocket, leetify/stat_bonus/absorbed map); runtime revision `phase4470`; RelayServer/JS/full TCRS derive remain non-goals.
- 2026-09-01 recount after Phases 4451–4460: 1,540 commonMain files / 8,512 tests / 1,035 ASH `regFn`; Behavioral Deepen VI (`is_goal`, `goal_exists` aliases, date/time ASH, `receive_fax`, `numeric_modifier` STAT/SERVANT/PHYLUM/familiar 4-arg); runtime revision `phase4460`; RelayServer/JS/full TCRS derive remain non-goals.
- 2026-09-01 recount after Phases 4441–4450: 1,539 commonMain files / 8,510 tests / 1,034 ASH `regFn` / 168 requests; Behavioral Deepen V + HTTP request residual (`current_maximizer_score()` no-arg, `goal_count("item"|"items")`, `SeaMerkinRequest`/`VolcanoIslandRequest`/`TrophyHutRequest`); runtime revision `phase4450`; RelayServer/JS/full TCRS derive remain non-goals.
- 2026-09-01 recount after Phases 4431–4440: 1,536 commonMain files / 8,493 tests / 1,034 ASH `regFn`; Behavioral Deepen IV + banish pattern closure (`is_banished(phylum)`, `goal_count("meat"|"level")`, alternate fight-HTML strings + `BANISHING_SHOUT` refuse guard); runtime revision `phase4440`; RelayServer/JS/full TCRS derive remain non-goals.
- 2026-09-01 recount after Phases 4371–4430: 1,535 commonMain files / 8,483 tests / 1,034 ASH `regFn`; Behavioral Deepen III (FightBanish `ORDER_A_KNEECAPPING`, outfit bitmap half-set bonuses, Breakfast jackass/sea-jelly, ASH corpus); RelayServer/JS/full TCRS derive remain non-goals.
- 2026-09-01 recount after Phases 4311–4370: 1,535 commonMain files / 8,463 tests / 1,034 ASH `regFn`; desktop r29219 combined modifiers (combined tags, maximizer succeeded, blue/red team, codpiece equip, TCRS notes); RelayServer/JS/full TCRS derive remain non-goals.
- 2026-09-01 recount after Phases 4251–4310: 1,535 commonMain files / 8,409 tests / 1,032 ASH `regFn`; behavioral deepen (goal_count, banish fallbacks, ascension summary, mall relay/search); RelayServer/JS/full TCRS derive remain non-goals.
- 2026-09-01 recount after Phases 4191–4250: 1,535 commonMain files / 8,407 tests / 1,032 ASH `regFn`; low-priority deepen-partials closure (GoalManager conditions, banish choice 191, mall fuzzy/relay, ascension snapshot); RelayServer/JS/full TCRS derive remain non-goals.
- 2026-09-01 recount after Phases 4131–4190: 1,579 commonMain files / 8,382 tests / 1,032 ASH `regFn`; non-goals partial closure (TCRS fetch, xpath, mall decorate); RelayServer/JS/full TCRS derive remain non-goals.
- Track G early ChoiceControl backfill Phases 877?888 adds Florist Friar, Tales of Dread, Folder Holder, K.R.A.M.P.U.S., Unpermery, Barely Tales, Odd Jobs, Survivor Encampment, Community Service, WLF Bunker, and choices 1229?1233 residual sync; ASH RuntimeLibrary mega gap-close Phases 889?949 (Tracks A?H) registers combat/choice, equip/familiar, prefs/counters, clan/shop/session, matcher/file/URL, sweet synthesis + skill costs, typed modifiers/eval/facts, and daily utils ASH; ASH deepen + residual Phases 950?1010 (Tracks I?T) deepen choice/synthesis/Track D writers/familiar lock/PHP LCG/modifier leftovers and register high-script residual ASH; CLI long-tail mega Phases 1011?1070 (Tracks A?F) wires IoTM/familiar/quest/session/oddball CLIs + help reconcile; Combat Encounter State mega Phases 1071?1130 (Tracks A?D) adds banish queueSize/phylum, TrackManager combat APIs + fight hooks, `canWalkAwayFromChoice`, adventure-loop glue; CCS / Macrofier mega Phases 1131?1190 (Tracks A?D) adds CustomCombatLookup section match, Macrofier expansion, live `run_combat(filter)`, adventure-loop CCS glue; EncounterManager + ResultProcessor mega Phases 1191?1250 (Tracks A?D) adds encounter classify/autostop/specials/wanderers, ResultProcessor loot?inventory, high-traffic gainItem; FightRequest residual mega Phases 1251?1310 (Tracks A?D) adds payActionCost/payItemCost, IoTM/familiar fight HTML handlers, updateFinalRoundData + ignoreSpecial clear, MonsterStatusTracker HP/Manuel; LimitMode Spelunky + Bat mega Phases 1311?1370 (Tracks A?D) adds LimitModeGates zone/skill/slot/meat/adventure + enter/exit, SpelunkyRequest, BatManager begin/end/stats/zone/fight; Fight Damage / Verse mega Phases 1371?1430 (Tracks A?D) adds FightCombatModeSync/FightDamageParser/FightVerseSync/FightCommentSync/FightDomSync + live won_initiative/monster_hp; Maximizer polish mega Phases 1431?1490 (Tracks A?D) adds Evaluator osity/weapon gates, HOLSTER/accessory DFS, Boost native horsery/boombox/mcd/cast/uneffect; Familiar message / lovebugs mega Phases 1491-1550 (Tracks A-D) adds FightLovebugSync full lb_* catalog, FightFamiliarMessageSync hub, FightFamiliarProgressSync final-round charges + adventure-loop glue; DiscoCombatHelper / rave combo mega Phases 1551-1610 (Tracks A-D) adds DiscoCombatHelper core, parseFightRound learn, Macrofier macroCombo + CCS disambiguate + adventure-loop glue; FightRequest DOM / session-log residual mega Phases 1611-1670 (Tracks A-D) adds FightSessionLog Round N:/After Battle:, FightProcessPSync prefs, FightNodeSync CLEESH/MONSTERID/rel= items + attribute/special-damage glue; ChoiceManager high-traffic long-tail mega Phases 1671-1730 (Tracks A-D) adds VioletFogManager, ChoiceCost/payCost, Louvre/Fighters/DeferredChoice + ChoiceAdventuresUsed; RequestLogger URL?session-log mega Phases 1731-1790 (Tracks A-D) adds session/RequestLogger.kt hub + shop/place/use/equip/skill/choice IoTM/coinmaster + long-tail visit glue; ResultProcessor gainItem mega Phases 1791-1850 (Tracks A-D) deepens ItemPool quest/IoTM/availability side effects + processItem/autoCreate; AdventurePrep / canAdventure mega Phases 1851-1910 (Tracks A-D) adds preValidate/tooDrunk + core/IoTM zone gates + prepare clusters; BetweenBattle / Recovery mega Phases 1911-1970 (Tracks A-D) adds runBetweenBattleChecks + pref scripts + residual prepare + LightsOutManager; Prepare HTTP / BetweenBattle edge mega Phases 1971-2030 (Tracks A-D) adds visitUrl/equipItem DI, PlaceRequest unlocks, consumable-entry prepare, RecoveryGates/autoAbort, BetweenBattleInvoker callers; UseItem / Consume response mega Phases 2031-2090 (Tracks A-D) adds UseItemConsumptionSync parseConsumption, eat/drink/chew organ sync, ItemMaximumUses depth, visit/queue glue; EquipmentManager / EquipmentRequest residual mega Phases 2091-2150 (Tracks A-D) adds EquipmentManager setEquipment/autoequip/canEquip/discard/transform, EquipmentRequest parseEquipmentChange + inv_equip visit glue, ResultProcessor autoequip, UseItem bootskin/folder/sticker gear arms, OutfitManager currentOutfit/canEquip; CreateItem / CraftResponse residual mega Phases 2151-2210 (Tracks A-D) adds CreateItemCraftSync parseCrafting, IoTM/camp create subclasses, box-servant repair + create session-log + CreateAbortGate; Selective GenericRequest abort/process hubs mega Phases 2211-2270 (Tracks A-D) adds RequestAbortGate, UseSkillSync, TransferItemSync, ResultProcessor meat/stat/familiar/adventure hubs + ResponseTextParser; ResponseTextParser + ResultProcessor residual mega Phases 2271-2330 (Tracks A-D) adds AccountSync/CharSheetSync, economy/inventory routers, Basement/arena/bounty + live BL, processGainLoss/discard/donations; CampgroundRequest / PlaceRequest residual mega Phases 2331-2390 (Tracks A-D) adds CampgroundSync parse/dwelling/workshed/rest/mushroom + PlaceRequest/PlaceSync whichplace routers + adventuresUsed/visit glue; UseSkillRequest / FamiliarRequest residual mega Phases 2391-2450 (Tracks A-D) adds UseSkillOptimize prepareTool, UseSkillSync deepen, FamiliarSync parseResponse/change/equip + visit glue; CharPane residual + ApiRequest status hub mega Phases 2451-2510 (Tracks A-D) adds CharpaneInteraction/avatar/title/canInteract, NC forcers/last adventure/familiar-Clancy-YouRobot pane, ApiStatusSync parseStatus effects/lastadv/equipment/coolitems/zoot + CharacterStatusRefresh/visit glue; InventoryManager / RetrieveItem residual mega Phases 2511-2570 (Tracks A-D) adds RetrieveSourceGates autoSatisfy + freepull-before-storage, useEquipped/familiar steal, RetrievePricing priceToMake/cheaperToBuy + deepen retrieve_price, InventoryRefresh/parseInventory; AdventureRequest formSource / place.php URL mega Phases 2571-2630 (Tracks A-D) adds ShadowRift enum + ingress routing, AdventureDatabase formSource URL map, AdventureFormBuilder/updateFields, AdventureManager place.php glue; DwarfFactory + Basement/Arcade residual mega Phases 2631-2690 (Tracks A-D) adds DwarfFactoryRequest prefs/parse + DwarfNumberTranslator/contraption/dwarf CLI, BasementSync autoSwitch/test classify/action/run, ArcadeRequest Jackass/turns + breakfast rewire; AdventureFailure / adventure-session residual mega Phases 2691-2750 (Tracks A-D) adds AdventureFailures 121-entry table + side-effect prefs + StopReason mapping, AdventureSession last/next/recordToSession, AdventureManager failure-before-parse glue; Clan request/manager residual mega Phases 2751-2810 (Tracks A-D) adds ClanManager lounge/rumpus/stash/member caches plus lounge/rumpus/member/hall/stash/log/war/buff request parsers and visit glue; runtime revision is `phase3110`.


