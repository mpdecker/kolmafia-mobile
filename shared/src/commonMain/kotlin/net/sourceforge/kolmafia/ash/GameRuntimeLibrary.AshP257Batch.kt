package net.sourceforge.kolmafia.ash

/** AshP257 — ElementEntityFields `$element[image]` bracket field. */
internal fun GameRuntimeLibrary.registerAshP257Batch(scope: AshScope) {
    // Behavior lives in ElementEntityFields + ElementNames.getImage.
}
