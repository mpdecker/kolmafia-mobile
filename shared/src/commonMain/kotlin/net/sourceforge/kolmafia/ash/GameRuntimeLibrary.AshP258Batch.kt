package net.sourceforge.kolmafia.ash

/** AshP258 — ModifierEntityFields `$modifier[name|type]` bracket fields. */
internal fun GameRuntimeLibrary.registerAshP258Batch(scope: AshScope) {
    // Behavior lives in ModifierEntityFields + ModifierNames.canonicalName/valueType.
}
