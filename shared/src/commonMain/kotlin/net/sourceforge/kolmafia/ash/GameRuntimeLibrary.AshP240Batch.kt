package net.sourceforge.kolmafia.ash

/** AshP240 — buffbots.txt known-bot registry loader. */
internal fun GameRuntimeLibrary.registerAshP240Batch(scope: AshScope) {
    // Behavior lives in BuffBotDatabase.loadRegistry.
}
