package net.sourceforge.kolmafia.ash

/** AshP249 — SkillEntityFields core metadata ($skill[id|name|type|level|image|traincost|class]). */
internal fun GameRuntimeLibrary.registerAshP249Batch(scope: AshScope) {
    // Behavior lives in SkillEntityFields + SkillDefinitionProxy + SkillCategory.
}
