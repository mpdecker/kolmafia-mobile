package net.sourceforge.kolmafia.ash

/**
 * AshP226 — UntinkerRequest HTTP + untinker CLI (CLI registered in cliDispatch).
 */
internal fun GameRuntimeLibrary.registerAshP226Batch(scope: AshScope) {
    // Marker batch — untinker behavior lives in UntinkerRequest + cliDispatch.
}
