package net.sourceforge.kolmafia.ash

/** AshP264 — BuffBot philanthropic offering resolution for `csend` meat transfers (infra-only batch). */
internal fun GameRuntimeLibrary.registerAshP264Batch(scope: AshScope) {
    // No public ASH registrations; desktop exposes getOffering only via send/csend CLI.
}
