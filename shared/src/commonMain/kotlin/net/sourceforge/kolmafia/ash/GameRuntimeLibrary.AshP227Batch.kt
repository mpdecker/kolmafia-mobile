package net.sourceforge.kolmafia.ash

/**
 * AshP227 — JunkListManager persistence (loaded on login via SessionManager).
 */
internal fun GameRuntimeLibrary.registerAshP227Batch(scope: AshScope) {
    // Marker batch — junk list lives in JunkListManager + SessionManager.load.
}
