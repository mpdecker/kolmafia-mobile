package net.sourceforge.kolmafia.ash

/**
 * AshP214 — validate v40 (is_coinmaster_skill + shoprows pref restore probes).
 */
internal fun GameRuntimeLibrary.registerAshP214Batch(scope: AshScope) {
    // Batch marker for Phase 209 is_coinmaster_skill validate + shoprows restore regression.
}
