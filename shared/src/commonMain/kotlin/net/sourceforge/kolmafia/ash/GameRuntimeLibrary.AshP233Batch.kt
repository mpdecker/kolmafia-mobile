package net.sourceforge.kolmafia.ash

/**
 * AshP233 — UntinkerRequest.completeQuest (Knoll + Degrassi Knoll Garage side-trip) + quest sync.
 */
internal fun GameRuntimeLibrary.registerAshP233Batch(scope: AshScope) {
    // Marker batch — behavior lives in UntinkerRequest, GoalManager.runSideTripForItem, cliDispatch.
}
