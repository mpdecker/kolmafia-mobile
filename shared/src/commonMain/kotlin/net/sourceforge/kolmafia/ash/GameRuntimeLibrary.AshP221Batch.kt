package net.sourceforge.kolmafia.ash

/**
 * AshP221 — get_related(item,"fold") ASH (fold chain positions via FoldGroupDatabase).
 */
internal fun GameRuntimeLibrary.registerAshP221Batch(scope: AshScope) {
    // Registered in registerRelatedFunctions (AshP220 batch).
}
