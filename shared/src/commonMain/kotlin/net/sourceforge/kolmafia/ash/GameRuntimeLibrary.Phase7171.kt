package net.sourceforge.kolmafia.ash

/**
 * Phases 7171–7190 — HTTP Residual LV Track B
 * (Hermit / Skeleton of Crimbo Past / Altar / Town Gift Shop visit deepen).
 *
 * Parent LV wrap bumps REVISION to phase7210.
 */
internal fun GameRuntimeLibrary.registerPhase7171(scope: AshScope) {
    // Track B is request parseResponse + visit hooks; no additional ASH surface.
}
