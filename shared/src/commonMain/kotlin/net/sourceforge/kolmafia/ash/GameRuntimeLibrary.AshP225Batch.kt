package net.sourceforge.kolmafia.ash

/**
 * AshP225 — PulverizeRequest tender-hammer + item retrieve preflight (behavior in PulverizeRequest).
 */
internal fun GameRuntimeLibrary.registerAshP225Batch(scope: AshScope) {
    // Marker batch — retrieve polish lives in PulverizeRequest.
}
