package net.sourceforge.kolmafia.ash

/** AshP255 — BountyEntityFields `$bounty[field]` bracket fields. */
internal fun GameRuntimeLibrary.registerAshP255Batch(scope: AshScope) {
    // Behavior lives in BountyEntityFields + BountyDatabase.
}
