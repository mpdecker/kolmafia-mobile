package net.sourceforge.kolmafia.ash

/**
 * AshP165 — collection ASH v1 (`get_free_pulls` / `get_cached_free_pulls` + tower cache invalidation).
 */
internal fun GameRuntimeLibrary.registerAshP165Batch(scope: AshScope) {
    // Batch marker for Phase 185 free-pull collection ASH wiring.
}
