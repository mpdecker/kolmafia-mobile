package net.sourceforge.kolmafia.ash

/** AshP241 — item entity bracket fields (consumable v1). */
internal fun GameRuntimeLibrary.registerAshP241Batch(scope: AshScope) {
    // Behavior lives in ItemEntityFields + ConsumableDatabase bracket lookups.
}
