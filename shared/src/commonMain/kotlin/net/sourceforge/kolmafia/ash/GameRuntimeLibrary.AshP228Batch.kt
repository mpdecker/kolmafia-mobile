package net.sourceforge.kolmafia.ash

/**
 * AshP228 — CleanupJunkRunner orchestration + cleanup/junk CLI (registered in cliDispatch).
 */
internal fun GameRuntimeLibrary.registerAshP228Batch(scope: AshScope) {
    // Marker batch — cleanup junk behavior lives in CleanupJunkRunner + cliDispatch.
}
