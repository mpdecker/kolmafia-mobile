package net.sourceforge.kolmafia.ash

/** AshP239 — nonfilling.txt consumable loader (level-req use items). */
internal fun GameRuntimeLibrary.registerAshP239Batch(scope: AshScope) {
    // Behavior lives in ConsumableDatabase.loadNonFilling + lookup helpers.
}
