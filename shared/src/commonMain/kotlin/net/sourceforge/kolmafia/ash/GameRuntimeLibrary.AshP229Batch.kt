package net.sourceforge.kolmafia.ash

/**
 * AshP229 — FlaggedItems singleton/memento list persistence (JunkListManager extension).
 */
internal fun GameRuntimeLibrary.registerAshP229Batch(scope: AshScope) {
    // Marker batch — flagged lists live in JunkListManager + SessionManager.load.
}
