package net.sourceforge.kolmafia.ash

/** AshP242 — item restore bracket fields (minhp/maxhp/minmp/maxmp). */
internal fun GameRuntimeLibrary.registerAshP242Batch(scope: AshScope) {
    // Behavior lives in ItemEntityFields + RestoreDatabase expression eval.
}
