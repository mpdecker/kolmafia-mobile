package net.sourceforge.kolmafia.ash

/** AshP251 — EffectEntityFields core metadata fields + statuseffects default_action load. */
internal fun GameRuntimeLibrary.registerAshP251Batch(scope: AshScope) {
    // Behavior lives in EffectEntityFields + EffectDatabase/EffectDefinitionProxy.
}
