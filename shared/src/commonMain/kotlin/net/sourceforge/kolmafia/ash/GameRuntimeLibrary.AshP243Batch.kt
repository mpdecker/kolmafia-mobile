package net.sourceforge.kolmafia.ash

/** AshP243 — item flag/property bracket fields (quest/gift/tradeable/usability/craft flags). */
internal fun GameRuntimeLibrary.registerAshP243Batch(scope: AshScope) {
    // Behavior lives in ItemEntityFields + ItemDatabase attribute helpers.
}
