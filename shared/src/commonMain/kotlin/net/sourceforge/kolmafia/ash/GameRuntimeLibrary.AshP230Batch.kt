package net.sourceforge.kolmafia.ash

/**
 * AshP230 — CleanupJunk singleton/memento polish + Pulverize junk auto-add + bare untinker CLI.
 */
internal fun GameRuntimeLibrary.registerAshP230Batch(scope: AshScope) {
    // Marker batch — behavior lives in CleanupJunkRunner, PulverizeRequest, UntinkerRequest, cliDispatch.
}
