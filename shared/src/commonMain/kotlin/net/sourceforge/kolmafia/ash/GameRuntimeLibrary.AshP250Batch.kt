package net.sourceforge.kolmafia.ash

/** AshP250 — SkillEntityFields tag/runtime fields + DailyLimitDatabase cast-by-skill-id index. */
internal fun GameRuntimeLibrary.registerAshP250Batch(scope: AshScope) {
    // Behavior lives in SkillEntityFields + DailyLimitDatabase.getCastPrefForSkill().
}
