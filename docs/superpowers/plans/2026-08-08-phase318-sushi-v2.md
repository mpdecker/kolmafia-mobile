# Phase 318 — SUSHI v2 (parseConsumption + visit registration)

**Track:** TCRS `applyModifiers` v59  
**Revision:** `phase318`

## Summary

Ports desktop `SushiRequest.parseConsumption()` / `registerRequest()` post-create side-effects into the mobile `SushiCreateRequest` automation path.

## Delivered

- `SushiChoiceMapper.resultNameFromFormFields()` — inverse form→result name mapping
- `SushiConsumptionSync` — ingredient `ItemConsumed` events, fullness bump when response lacks `Fullness`, fancy doily (6328), worktea clue pref + consume (6356), session-log line
- `SushiCreateRequest` — injects `InventoryManager`/`KoLCharacter`/`SessionLogger`/`Preferences`/`GameEventBus`; calls sync after successful POST + `fetchInventory()`
- Tests: `SushiConsumptionSyncTest`, extended mapper/request tests

## Deferred Phase 319+

- Meat-paste COMBINE/JEWELRY preflight in `CreateItemIngredients`
- Muse/Vykea/Terminal/Sewer `makeIngredients` wiring
- Full `DreadScrollManager`
- Manual browser `sushi.php` visit hook
