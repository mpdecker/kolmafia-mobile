# HTTP Residual Mega L (6851–6910)

> **For agentic workers:** Do NOT bump `REVISION` until parent wraps to `phase6910`. Exclude Relay/JS/TCRS derive. Prefer extending `CoinmasterAccessibility` via focused helpers.

**Goal:** Close remaining high-ROI coinmaster `accessible()` gaps after XLIX (path/quest).

**Desktop:** `coinmaster/shop/*Request.java` + `BURTRequest` / `FudgeWandRequest` / `HermitRequest` / `GameShoppeRequest`  
**Mobile tip:** `phase6850` → wrap `phase6910` ✅

## Tracks

### Track A — Specialty IoTM / zone shops (6851–6870)
`SpecialtyShopAccessibility.kt` + wire:

| Nickname | Gate |
|---|---|
| `shadowforge` | `lastShadowForgeUnlockAdventure == currentRun` |
| `fantasyrealm`, `rubee` | `frAlways` / `_frToday` |
| `cyber_dedigitizer`, `dedigitizer` | `crAlways` / `_crToday` |
| `sandpenny`, `wetcrap` | `inSeaPath` |
| `topiary`, `nugglet` | topiary nugglet **7968** |

### Track B — Legacy token + Mr Store 2002 (6871–6890)
`LegacyCoinmasterAccessibility.kt` + wire:

| Nickname | Gate |
|---|---|
| `mrstore2002` | catalog **11257** or LoL replica **11280** |
| `burt` | BURT token **5683** |
| `fudge`, `fudgewand` | wand **5441** + fudgecule **5435** |
| `hermit` | not KoE |
| `gameshoppe`, `gamestore` | not HC/Ronin |

### Track C — Tests + corpus + parent wrap (6891–6910)
Markers, corpus Tracks A–C, bump `phase6850`→`phase6910`, audit/AGENTS.

## Non-goals
Relay/Generic/Purchase; inventing ASH; full NPC inventory parse sweep; Crimbo seasonal one-offs.
