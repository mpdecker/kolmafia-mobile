# HTTP Residual Mega LI (6911–6970)

> **For agentic workers:** Do NOT bump `REVISION` until parent wraps to `phase6970`. Exclude Relay/JS/TCRS derive.

**Goal:** Close leftover high-ROI coinmaster `accessible()` gaps after L (Island War shops + residual legacy).

**Mobile tip:** `phase6910` → wrap `phase6970`

## Tracks

### Track A — Island War shops (6911–6930)
`IslandWarShopAccessibility.kt` + replace wrong dimemaster king-liberated gate:

| Nickname | Gate |
|---|---|
| `dimemaster` / `dmt` | `warProgress=started` + War Hippy Fatigues pieces |
| `quartersmaster` | `warProgress=started` + War Frat Ouetfit pieces |

Messages: `You're not at war.` / `You don't have the War Hippy Fatigues` / `You don't have the Frat Boy Ensemble`

### Track B — Residual legacy shops (6931–6950)
`ResidualLegacyShopAccessibility.kt`:

| Nickname | Gate |
|---|---|
| `awol` | AWOL commendation **5116** |
| `bigbrother` | `bigBrotherRescued` or bubblin' stone **3605** (+ scuba gear for underwater) |
| `dino` / `dinostaur` | `inDinocore` |
| `crimbo17` | crystalline cheer **9625** |
| `crimbopast` / `socp` / `skeleton` | owned familiar id **326** |
| `altar` / `bonealtar` / `bones` | always `"The Altar of Bones is not available"` (desktop parity) |

### Track C — Tests + corpus + parent wrap (6951–6970)
Markers, corpus, bump `phase6910`→`phase6970`, audit/AGENTS.

## Non-goals
Relay/JS/TCRS; full BigBrother equip HTTP; Crimbo23 seasonal deepen.
