# HTTP Residual Mega LII (6971–7030)

> **For agentic workers:** Do NOT bump `REVISION` until parent wraps to `phase7030`. Exclude Relay/JS/TCRS derive.

**Goal:** Close leftover high-ROI coinmaster accessible + manager DI + Traveling Trader parse after LI.

**Mobile tip:** `phase6970` → wrap `phase7090`

## Tracks

### Track A — Spaaace + Time Tower polish (6971–6990)
- `SpaaaceShopAccessibility` / wire `elvishp1`/`isotopesmithery`, `elvishp2`/`dollhawker`, `elvishp3`/`lunarlunch` → `SpaaaceRequest.accessible` (desktop: generator **finished** + Transpondent effect **846** or transponder **5170**)
- Align mobile `SpaaaceRequest.accessible` with desktop finished-only gate
- `TimeTowerAccessibility`: add `twitch_jousting` → `"You can't get to the Renaissance Gift Shop"`
- CoinmasterDatabase nickname aliases for Spaaace shops

### Track B — Manager DI + Traveling Trader (6991–7010)
- `CoinmasterManager`: pass `hasEffect` from `EffectManager`, `ownsFamiliar` (already), Spaaace quest via `QuestDatabase`
- Underwater flags for Big Brother: treat equipped/inventory scuba as before; optionally effect/path via callbacks when EffectManager present
- `TravelingTraderRequest.parseResponse` deepen: acquire token pref + dynamic buy rows via `CoinmasterVisitInventory` (`trader` / `traveler`)
- Wire Crimbo11/14/16 hubs `parseResponse` → `CrimboHubResponseParse` when missing

### Track C — Tests + corpus + parent wrap (7011–7030)
Markers `Phase6971`/`Phase6991`/`Phase7011`, tests, corpus, bump `phase6970`→`phase7090`, audit/AGENTS.

## Non-goals
Relay/JS/TCRS; full Traveling Trader inventory/storage delta accounting; Crimbo seasonal UI.
