# HTTP Residual Mega XLIX (6791–6850)

> **For agentic workers:** Do NOT bump `REVISION` (parent wraps to `phase6850`). Exclude Relay/JS/TCRS derive. Prefer extending `CoinmasterAccessibility` via focused accessibility helpers over new per-shop Request classes.

**Goal:** Close remaining high-ROI coinmaster `accessible()` gaps left after XLVIII (airport/token/KOLHS/Bat downtown).

**Desktop:** `coinmaster/shop/*Request.java` accessible()  
**Mobile tip:** `phase6790` → wrap `phase6850` ✅

## Tracks (parent implements; agents optional)

### Track A — Path / pref shop accessibility (6791–6810)
`PathShopAccessibility.kt` + wire nicknames:

| Nickname | Gate |
|---|---|
| `olivers` / speakeasy | `ownsSpeakeasy` |
| `exploathing` | `isKingdomOfExploathing` |
| `pokefam` | `inPokefam` |
| `mutate` | `inNuclearAutumn` |
| `mariogear` / `marioitems` | plumber path |
| `edunder_shopshop` | Ed path + limitMode ED |
| `detective` | `hasDetectiveSchool` |
| `rumple` | `grimstoneMaskPath == gnome` |
| `spacegate` | `_spacegateToday` / `spacegateAlways` |
| `campfire` | CampAwayAvailability.campAwayTentAvailable |
| `cindy` | inventory odd silver coin 7144 |

### Track B — Quest / Bat fabricator / Fun-a-Log (6811–6830)
`QuestShopAccessibility.kt` + bat fabricator:

| Nickname | Gate |
|---|---|
| `grandma` | SEA_MONKEES later than step8 |
| `blackmarket` | MACGUFFIN available (step1+) |
| `piraterealm` | Fun-a-Log item in inventory (FunALogUnlockPrefs) |
| `batman_cave` | BATMAN + Bat-Cavern (BatCoinmasterAccessibility) |

### Track C — Tests + corpus + parent wrap (6831–6850)
- Markers `Phase6791` / `Phase6811`
- Tests + corpus Tracks A–C
- Parent: bump `phase6790`→`phase6850`, audit, AGENTS

## Non-goals
Relay/Generic/Purchase; inventing ASH; full NPC inventory parse sweep (defer to L).
